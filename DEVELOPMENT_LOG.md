# PumpSignal development log

This is the agent's chronological external memory. Append every material change; do not rewrite history to match an assumption.

## V5 research mode — three autonomous silent paper systems (2026-08-16)

- Enabled a single fail-closed `ResearchModePolicy`: APP, DeepSig and DeepSigX continue evaluating and paper trading, while signal/trade/reminder/position notifications, vibration and sound tests are suppressed. The required Android foreground monitor notification remains low-importance and explicitly identifies the silent test.
- Replaced the APP paper-account decision path with the causal V5 research engine: regime separation, trend pullback/range reclaim/breakout retest, anti-chase filtering, buyer-flow completeness, precomputed entry zone, stored ATR/invalidation and structural/ATR exits instead of a fixed 2% reaction.
- Started a clean V5 research epoch in three separate preferences stores. Existing V4.22 paper histories are left untouched as an archive and are not mixed into the new comparison.
- Made DeepSig an autonomous LLM challenger: APP and user thresholds have no execution authority, two independent persistence checks and a second risk-verification call remain mandatory, and ordinary 2% noise is not an exit rule.
- Made DeepSigX autonomous: it starts from its own €1,000 portfolio, does not mirror DeepSig, ignores APP/DeepSig as entry triggers and uses its own breathing/flow/CVD/structure evidence with three spaced confirmations.
- Updated the user-facing comparison to rank only APP, DeepSig and DeepSigX; Serge remains a separate manual reference. Added regression tests for silent mode, independent DeepSigX sources, APP risk-contract persistence and entry-zone execution.
- `git diff --check` passes. Unit tests could not start in this managed workspace because Gradle 8.5 is not cached and `services.gradle.org` is unreachable; CI compilation/test/lint remains required before producing or installing an APK.

## Recovered baseline

- Project repository: `rosergenbg-debug/pump-paper-bot-android`. Never modify the old `price-tracker-android` project.
- StrategyV2 and Gemini use separate virtual portfolios. User/Serge is the third independent virtual portfolio.
- Gemini runs as an hourly independent Shadow experiment. It receives market/news context, uses a fresh post-response execution quote, and does not control StrategyV2.
- Monitoring uses a foreground service near every two minutes plus WorkManager reserve cycles. Ordinary alerts include quiet-hour scheduling; urgent personal exits may bypass it.
- Signing history caused failed Android updates. Compatible release certificate SHA-256 is `1f778c4291c9…27dc7823`; GitHub intermediate builds previously used a different certificate and must not be handed to Serge as final updates.

## V3.18 — three-way competition

- Added APP, Gemini and SERGE virtual competition, balances, signed returns and graph trade markers.
- Correct release is V3.18/code 50 over V3.17/code 49. Earlier temporary V3.19 labels on the same feature were a numbering mistake and were reverted.
- Final V3.18 APK was signed with the compatible certificate and delivered as a direct APK.

## V3.19 — trade alerts and timing tuning

- Request: always ring for APP entry, APP exit, Gemini entry and Gemini exit. Gemini entry timing is good; Gemini exit is late. APP exit is good; APP entry is too rare.
- Added fresh high-importance alarm channels for APP trades and Gemini trades. BUY and SELL use distinct notification ids, so APP and Gemini events cannot overwrite one another.
- Alerts are triggered by newly appended executed trades, not only by a readiness gauge. When an APP trade alert fires, the generic APP readiness alert is suppressed for that cycle to avoid a duplicate ring.
- Replaced Gemini BUY-only alert detection with BUY/SELL trade detection.
- Initial implementation added fixed Gemini exits at −3% or after +2% and a 1.2% pullback. Serge rejected fixed percentages as the main decision logic before release, so this was removed from the control Gemini.
- Modestly widened APP entry confirmation: cautious RSI recovery accepts up to 57; Active mode keeps a valid RSI recovery for one extra closed candle, accepts a wider RSI band and slightly lower shock-volume threshold. Late-entry, rapid-drop and PUMP/BTC/SOL overheat vetoes remain unchanged.
- Added unit coverage for APP/Gemini executed-trade detection, Gemini protective exit and APP entry sensitivity.
- Release metadata set to V3.19/code 51. Final build/signature verification remains required before delivery.
- Local commit `83ea1ae` was created on `agent/v3-19-trade-alerts-and-tuning`. Push was blocked by the workspace publication guard pending explicit user approval for this exact repository/branch; no remote V3.19 branch or CI build exists yet.

## V3.19 — Gemini exit experiment

- Request: add a fourth visible participant, «Gemini‑эксперимент», alongside APP, Gemini and Serge. It must send its own entry and exit signals and be visible in a four-way comparison.
- Kept Gemini as the control. Removed the unreleased fixed-percentage protective exit and the prompt wording that would have changed Gemini's exit behaviour.
- Added a separate persistent experimental portfolio. On first run it starts from an exact checkpoint copy of Gemini; afterwards it mirrors only Gemini's newly executed BUY at the same quote. Gemini SELL does not close the experiment.
- The experimental exit checks every monitor cycle. Evidence groups are adaptive price pullback, spot/futures buyer flow, spot/futures CVD, BTC/SOL, open interest, order-book imbalance and the existing market-direction score. A moderate multi-group reversal must persist across two checks; a strong reversal can exit immediately. A −5% loss is only the emergency backstop.
- Added distinct high-importance alarm notifications «GEMINI‑ЭКСПЕРИМЕНТ: ВХОД/ВЫХОД», separate ids and a separate Android channel.
- Main screen now uses a 2×2 account grid. The comparison screen renders four synchronized graphs. A dedicated experiment screen shows balance, return, position, evidence score, confirmation streak, adaptive noise allowance and trades.
- Added pure unit coverage for checkpoint initialization, mirrored entry, ignored control SELL, two-cycle confirmation, isolated-indicator rejection and emergency backstop.

## V3.19 — build and compatible APK

- Serge explicitly authorized publishing `agent/v3-19-trade-alerts-and-tuning` to `rosergenbg-debug/pump-paper-bot-android` and running the V3.19 build on 2026-08-01.
- Published the exact local V3.19 tree `43140c1fb6e4967b1bdb3aaa31ff69cc197330e0` through GitHub. Remote branch head after reconciling the independent `main` history: `ce24b2b9bdec89f303e58d198a294a9cdf45225f`.
- Opened draft PR #28 only as the safe Android Build trigger. GitHub Actions run #109 completed successfully, including unit tests, `assembleDebug`, package/version/activity checks and intermediate APK verification.
- Re-signed the verified intermediate APK with the installed-compatible personal update key. Final certificate SHA-256: `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`; APK Signature Schemes v2 and v3 verify successfully.
- Final APK: `PumpSignal-V3.19-Compatible-FINAL.apk`, 7,047,471 bytes, SHA-256 `e6d48beb303a38e5e837771ffb6029bbced7c79ed625708e3f48ec0ad14e4bf9`. Its `AndroidManifest.xml` and `classes.dex` hashes exactly match the CI-verified intermediate APK.

## V3.20 — attributed signals and earlier Gemini experiment entry

- Serge reported a correctly timed preparatory signal followed by a strong rise, but could not tell which participant generated it or why no virtual account bought. He requested a persistent top-screen source/reason label and a moderately earlier entry for Gemini experiment.
- Root cause confirmed in V3.19 code: Gemini experiment had no independent entry path. While in EUR it could only wait for and mirror an already executed control-Gemini BUY; visible positive gauges/evidence could never open its portfolio.
- Added a persistent top signal attribution panel. It records source, signal/trade type, full reason, time and whether a virtual trade was actually executed. APP 99/100 is explicitly marked as no trade; APP, Gemini and Gemini-experiment trades are distinct.
- Added an independent early-entry path for Gemini experiment. Its anchor is APP readiness 99/100 or a fresh positive Gemini direction (at least +20 direction and 55 confidence). Entry additionally requires confirmed PUMP momentum and either aligned spot/futures buyer flow or positive spot/futures CVD.
- Preserved hard entry vetoes for late price, broad overheat, unconfirmed rapid drop, simultaneous BTC/SOL weakness and missing independent buyer confirmation. A blocked signal remains visible with its exact reason.
- Kept control-Gemini entry mirroring as a fallback and kept all portfolios independent. Existing V3.19 experiment state remains readable; new `lastPhase` state defaults safely for old JSON.
- Added unit cases for confirmed independent entry, visible non-executed preparation and late-entry veto. Release metadata advanced once to V3.20/code 52; build and final compatible-signature verification remain pending.
- Changes were committed locally on `agent/v3-20-signal-source-and-early-entry` as `d9f8f1f`. The worktree was clean after the commit. Full Android compilation remains pending because this workspace has neither Gradle nor the required authenticated `gh` CLI.

## V3.21 — APP confirmed trend continuation

- Serge reported that APP remained in EUR throughout a long, orderly rise even though the visible market setup looked ideal. Code review confirmed that every APP entry path required a preceding RSI dip, shock or multi-drop exhaustion; there was no continuation entry for a clean rising trend.
- Added a separate continuation path that requires two completed rising 30-minute candles above a rising EMA20, RSI 47–60, safe distance from EMA200, no three-hour chase, positive BTC trend and a non-weak broad market.
- Buyer confirmation is mandatory: aligned non-negative spot/futures taker imbalance, or stronger spot imbalance with non-negative relative-strength slope. The old late-entry, broad overheat and rapid-drop vetoes remain in force.
- Funding up to and including `+0.01%` is treated as neutral (`0.0001` raw), fixing the prior rejection of a harmless `+0.005%` rate. The same neutral threshold now applies to the original trend-recovery path.
- The new entry opens the existing `TREND` position mode so saved-state compatibility and the established APP exit remain unchanged. Continuation readiness contributes a visible 99/100 preparatory signal when exactly one independent confirmation is still missing.
- Added focused unit coverage for the funding boundary, two-candle confirmation, RSI band and buyer-flow alternatives. Release metadata advanced once to V3.21/code 53; README, UI title and GitHub workflow checks/artifact names were updated consistently.
- Static diff/XML checks pass. Full Gradle unit tests, APK assembly and compatible-certificate verification remain pending because `gh` is not installed in the workspace; the mandatory GitHub publish procedure requires authenticated GitHub CLI and forbids bypassing this prerequisite.

## V3.21 — build and compatible APK

- Published the exact local V3.21 tree `38d20a82dbd1e19f0271dc08843888179585621f` to `agent/v3-21-app-trend-continuation`; the remote tree matches local commit `8cdec01` byte-for-byte.
- Opened draft PR #29 as the Android Build trigger. GitHub Actions run #111 completed successfully, including unit tests, `assembleDebug`, package/version/activity checks and intermediate APK verification.
- Re-signed the CI APK with the installed-compatible personal update key. Final certificate SHA-256: `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`; APK Signature Schemes v2 and v3 verify successfully.
- Final APK: `PumpSignal-V3.21-Compatible-FINAL.apk`, 7,059,759 bytes, SHA-256 `a8d22bd12834c2eab7e44250a1d47097d702a15723589df27b8048e112fcc4be`. Its `AndroidManifest.xml` and `classes.dex` hashes exactly match the CI-verified intermediate APK.

## V3.22 — full audit fixes

- Serge requested one combined release from the full V3.21 control-flow audit and the separate visible-text audit, without changing trading thresholds.
- Added persistent per-participant trade-alert outboxes. APP, Gemini and Gemini-experiment now commit the new portfolio and pending trade alert together before attempting the Android notification. A failed/disabled notification remains queued and is retried in later cycles; the virtual trade itself is not repeated.
- Android notification permission and app-level notification availability are checked before a trade alert is considered accepted by the notification manager.
- Gemini-experiment entry now reads readiness from `PumpBotEngine.evaluateAppPaper()` with the independent APP portfolio. It no longer uses the manual Serge position's `waitMode/readinessScore`. The experiment also reconciles the latest control-Gemini trade each cycle so an interruption between control BUY persistence and mirroring cannot permanently drop that command.
- APP, Gemini and Gemini-experiment stores now keep a last-good JSON backup. If both primary and backup data are unreadable, automated trading stops with an explicit storage error instead of silently replacing history with a fresh €1,000 account.
- Ordinary quiet hours now apply every day and migrate the old default start from 06:00 to 06:15. Executed trade alerts and urgent exits remain immediate. Settings and status text were updated accordingly.
- User-visible release headers now use `BuildConfig.VERSION_NAME`; stale V3.15/V3.4 headers, radar explanations, reset wording and the old three-account details screen were updated for the four-participant architecture.
- Internal network identification now uses the build version instead of hard-coded `3.18`.
- Removed the personal key fetch and plaintext signing password from the active GitHub workflow. CI produces and verifies only an intermediate debug APK; the installed-compatible personal signature is applied after CI verification.
- Release metadata advanced once to V3.22/code 54. Added focused unit coverage for the 06:15 schedule boundary and independent APP readiness. Full CI build, APK inspection and compatible signature verification are pending.
- GitHub Actions run #113 reached Kotlin compilation and exposed two build-only type issues: BuildConfig generation was disabled and storage-error message lambdas returned nullable strings. Enabled `buildConfig` and made all four messages non-null; no strategy logic changed. A clean CI rerun is required.
- Published the corrected tree `c73e4a260617caa011209b9661d0bce95e586ee5` to `agent/v3-22-audit-fixes`. GitHub Actions run #114 completed successfully: all unit tests, Kotlin/Android compilation, `assembleDebug`, package `com.example.pumppaperbot.v8`, V3.22/code 54, launch activity and APK integrity checks passed.
- Downloaded artifact `PumpSignal-V3.22-Intermediate-APK` from run #114; its ZIP digest is SHA-256 `c5d21e06836f8254338ed6de260805d705003a74e4b7a6256fc329152916e555`.
- Re-signed the verified intermediate APK with the installed-compatible personal update key. Final certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V3.21; APK Signature Schemes v2 and v3 verify successfully.
- Final APK: `PumpSignal-V3.22-Compatible-FINAL.apk`, 6,989,928 bytes, SHA-256 `07dfbaa7b160c427205d89dd881684b88fa8db9228f332adbb956521b8d99e3d`. Its `AndroidManifest.xml` and `classes.dex` hashes exactly match the CI intermediate APK.

## V4.0 — protected API keys and Serge position supervision

- Serge requested that a saved API key never remain in an editable field. Replaced the always-visible Gemini input with explicit provider panels: initially only «Ввести API-ключ» is shown; after save the input disappears and only «Изменить API» and «Удалить» remain. Deletion requires a confirmation dialog and a blank edit can no longer overwrite a valid key.
- Added a separate Android-Keystore-backed DeepSeek credential store and excluded it from cloud backup and device transfer. Gemini migration/storage remains compatible and neither key is embedded in the APK or repository.
- Added DeepSeek supervision for Serge's actual manual position. `deepseek-v4-pro` is forced immediately after «Я купил» and used for critical follow-ups; `deepseek-v4-flash` handles ordinary 15-minute position checks. Existing free Gemini hourly/news circuits remain independent to conserve their limited quota.
- The supervisor persists the first exit recommendation and continues checking when Serge does not sell. It shows change relative to that exit baseline on a −10…+10 scale, a separate 0…10 danger level, worsening/improvement messages and an explicit `CANCEL_EXIT` state.
- Added a dedicated high-importance Android alarm channel for initial exit, later deterioration/improvement and exit cancellation. Position supervision state is reset only for a new/closed/reset Serge position and is compatible with a V3.22 update.
- Release metadata advanced once to V4.0/code 55; package id remains `com.example.pumppaperbot.v8`. README and CI artifact/badging checks were updated. Focused state/scale/status unit tests were added; full CI build and compatible final signature verification are pending.
- Work is isolated on local branch `agent/v4-deepseek-position-supervisor`. Static diff and secret scans pass. This environment has no Gradle executable, XML validator or required `gh` CLI, so compilation, GitHub publication and CI APK verification remain explicit blockers rather than assumed successes.
- Created local implementation commit `6c1dac3` on `agent/v4-deepseek-position-supervisor`; the worktree was clean immediately after the checkpoint. It has not been pushed because the mandatory GitHub CLI prerequisite is unavailable.
- Serge explicitly authorized direct GitHub publication and the V4.0 build. Published the exact local V4 tree `82306340b1d9892fb06b62f162098bd0ed95464a` to `agent/v4-deepseek-position-supervisor` through the connected GitHub app as remote commit `55d40cb7dce87b4aac643d6dcee2ba8c27098c46`, then opened draft PR #31 as the Android Build trigger.
- GitHub Actions run #117 (`30700745815`) completed successfully. Unit tests, Kotlin/Android compilation, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.0/code 55, launch activity, APK Signature Scheme v2, archive integrity and the minimum-size check all passed.
- Downloaded artifact `PumpSignal-V4.0-Intermediate-APK`; its ZIP is 6,030,790 bytes with SHA-256 `72804bef21583ca7d7c5a6710b6169564501c9c382e80da786e97dcb159515a7`.
- Re-signed the CI APK with the installed-compatible personal update key. Final certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`; APK Signature Schemes v2 and v3 verify successfully.
- Final APK: `PumpSignal-V4.0-Compatible-FINAL.apk`, 7,088,431 bytes, SHA-256 `138902fcacf8a80edb191674a9ed2f3a477ea89383a1e3c95bb6fdde7c177397`. Its `AndroidManifest.xml` and `classes.dex` hashes exactly match the CI-verified intermediate APK.

## V4.1 — DeepSeek primary circuit and Gemini position reserve

- Serge reported that the V4.0 screen still presented Gemini as the main worker and did not prove that DeepSeek was processing anything. Code inspection confirmed the root cause: DeepSeek was only called after Serge pressed «Я купил»; before an open position it performed no market or news analysis.
- Added an independent primary DeepSeek Flash circuit. It receives the latest PUMP/EUR market frame, order-book/OI fields, local APP evidence and up to five current radar headlines every ten minutes, whether or not Serge has an open position. Gemini failures are recorded separately and do not block this circuit.
- Kept the position supervisor separate and stronger: DeepSeek Pro is forced immediately after «Я купил» and on critical follow-ups; ordinary primary Flash analysis continues alongside it.
- Added a compact first-screen «DEEPSEEK • ОСНОВНОЙ» card showing the active model, action, short conclusion, today's successful requests/errors and last successful response time. The radar screen also exposes the same real counters.
- Replaced the former internal Gemini ceiling of 50 with a conservative observed budget of 20 daily attempts: 10 are available outside a Serge position and 10 remain reserved until a real position is open. Once the ordinary half is consumed, retry scheduling sees zero available ordinary calls instead of generating repeated blocked attempts.
- Official DeepSeek V4 documentation confirmed both model ids but exposed a V4.0 request-shape bug: `reasoning_effort` had been nested inside `thinking`. Both primary and position clients now send `thinking: {type: enabled}` plus top-level `reasoning_effort`, matching the documented Chat Completions request. Pro receives a larger output allowance for critical reasoning.
- Release metadata advanced once to V4.1/code 56. Package id and protected stores are unchanged, so V4.0 data remains update-compatible. README and CI badging/artifact checks were updated; focused unit tests cover position-independent DeepSeek scheduling, the ten-minute interval and the 50/50 Gemini split.
- Static diff/whitespace checks pass. Local Gradle compilation is unavailable because the repository intentionally has no wrapper and this workspace has no Gradle executable; GitHub Actions compilation, APK inspection and compatible signing remain pending.
- GitHub Actions run #119 reached Kotlin compilation and found one missing closing brace after the `DeepSeekPrimaryPolicy.compactStatus` `when` expression. Added only the missing delimiter; the resulting retry must rerun the full test/build/inspection workflow.
- Published the focused syntax fix as remote commit `9c0e86af3ff6dc4053782504badd0ecbb0be1a15`. GitHub Actions run #120 (`30703376353`) completed successfully: unit tests, Kotlin/Android compilation, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.1/code 56, launch activity, APK Signature Scheme v2, archive integrity and size checks all passed.
- Downloaded artifact `PumpSignal-V4.1-Intermediate-APK`; ZIP size is 6,041,924 bytes, SHA-256 `c76f3d6e23de1cfeff64e361464e2aa55b03c98d3e0f4ac85f62ec17cf16c1ae`. Intermediate APK SHA-256 is `dbfcdb38df5858dea92c9cb058c41943174d4f1283b73bbca4de50b1667a25b4`.
- Re-signed the verified intermediate with the installed-compatible update key. Final certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.0; APK Signature Schemes v2 and v3 verify successfully.
- Final APK: `PumpSignal-V4.1-Compatible-FINAL.apk`, 7,096,623 bytes, SHA-256 `1d14832638044339efadf943eaf498ba0a4d87d22c91b6ea98a9021323ad5362`. Its `AndroidManifest.xml` and `classes.dex` hashes exactly match the CI-verified intermediate APK.

## V4.2 — API centers, DeepSeek signal and richer analysis

- Serge requested replacing the two lower «Покупка/Продажа» indicators with DeepSeek API and Gemini API buttons. Added dedicated provider centers with protected key create/change/delete UX, live connection state, real manual tests, actual request rates for the last minute/hour/day, token totals and a capped seven-day request journal. Key controls and manual Gemini testing were removed from the news-radar screen.
- Kept the four virtual portfolios unchanged. Replaced only the old second signal gauge: APP remains the first independent signal and DeepSeek is now the second. Gemini remains a separate virtual control participant and reserve expert; its portfolio, trades and research history are not migrated into DeepSeek.
- Primary DeepSeek Flash cadence changed from ten to five minutes. A material readiness change of at least 15 points or a BUY/SELL state transition triggers an earlier call, and the main «Проверить» button explicitly requests a fresh primary analysis. Position Pro remains immediate after «Я купил» and on critical follow-ups.
- Audit found the V4.1 DeepSeek primary prompt materially narrower than the Gemini hourly prompt. Added PUMP 1h/3h/6h, BTC/SOL 1h/3h, spot/futures taker flow and CVD, premium and the existing funding/order-book/OI/RSI/StrategyV2/news data. The position supervisor receives the same evidence groups plus realized volatility. Responses now persist short evidence and invalidation risks.
- Gemini's internal budget follows Serge's observed 25-request allowance: 12 requests are available normally and 13 more unlock for an open Serge position. Outside a position, the autonomous expert is limited to one successful routine review per two hours; position-open and explicit manual calls bypass this cadence while still respecting the total quota. A 429 response that explicitly names a daily/RPD quota now pauses Gemini until the next Pacific-time reset instead of retrying after a short generic backoff.
- Added unified local API telemetry for DeepSeek primary/position calls and Gemini hourly/news calls. No API key, prompt payload, account balance or private market history is written to this journal.
- Release metadata advanced once to V4.2/code 57; package id remains `com.example.pumppaperbot.v8`. README, manifest and CI artifact/version checks were updated. Local static checks are pending before GitHub publication and full Android CI.
- Published exact V4.2 tree `03e97379d9a2738ba8285309fff6e0f5c9a2ba37` as remote commit `e7f2cb2983d0549eb34f2f4627b3bc7de856e3be` in draft PR #31. GitHub Actions run #122 (`30706506693`) completed successfully: unit tests, Kotlin/Android compilation, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.2/code 57, launch activity, intermediate APK signature, archive integrity and size checks passed.
- Downloaded artifact `PumpSignal-V4.2-Intermediate-APK`; ZIP size is 6,052,406 bytes, SHA-256 `8ce103f9c6b1a3ddf8884cc90489fc2694e4f1363cf1f454f36754f7881c61dd`. Intermediate APK SHA-256 is `f781e7d56e203b3e8c0cfce91c7f7ba67778f82268e841e1e3665d40aaec119f`.
- Re-signed the CI APK with the installed-compatible update key. Final certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.1; APK Signature Schemes v2 and v3 verify successfully.
- Final APK: `PumpSignal-V4.2-Compatible-FINAL.apk`, 7,034,984 bytes, SHA-256 `11992f4b1818eb2229f858726e621ee1f25a4852aa19a59b2d1a41eb85646a04`. Its `AndroidManifest.xml` hash `9cf48cda…15adf287` and `classes.dex` hash `f4a3aab8…e45eb448` exactly match the CI intermediate APK.
- Final pre-CI review added missing Gemini news-token telemetry, corrected the never-run DeepSeek next-analysis display, and added unit coverage that distinguishes daily/RPD quota exhaustion from short rate limits.
- Follow-up run #123 compiled the complete Android application, but the new quota test exposed Google's compact quota-id spelling `GenerateRequestsPerModelPerDay`. Added the compact `permodelperday` form to the daily-limit detector; a clean full rerun is required before delivery.
- GitHub Actions run #125 passed all 99 unit tests and the complete V4.2 Android build at commit `ea29ee974bf9d028db3cd31a239018a135db2734`; artifact `PumpSignal-V4.2-Intermediate-APK` ID `8820630404` has archive SHA-256 `bae8899586bac28b92c468566db34088238f5f3e637a7f30bf7dd65d29527407`.
- Re-signed the run #125 APK with the installed-compatible update key. Final `PumpSignal-V4.2-Compatible-FINAL.apk` is 7,034,984 bytes with SHA-256 `b816c3e4e4f528f8a50b4cd64817078f78352602adf5bad231b48667de198f43`; certificate SHA-256 remains `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, v2/v3 verify, and `AndroidManifest.xml` plus all three DEX entries exactly match the CI artifact.

## V4.3 — freshness-labelled DeepSeek market frame

- Serge asked whether DeepSeek truly receives the current order book, buying/selling pressure and other live inputs without hidden delay, and authorized a free, safe speed improvement if needed.
- Audit confirmed that the two-minute market cycle already refreshed a public Binance top-20 spot depth snapshot and present futures OI, but DeepSeek labelled the last closed 30-minute PUMP/EUR candle as its current price. Its taker-flow/CVD fields were aligned to the last full hour and could be almost one hour old.
- The foreground service already maintained a free Binance spot `aggTrade` plus `bookTicker` WebSocket and saved a micro snapshot every 15 seconds, but neither the primary DeepSeek analyst nor position supervisor consumed it. The existing closed five-minute spot/futures impulse layer was likewise mostly absent from their prompts.
- Added a separate freshness-labelled DeepSeek context. It supplies the live PUMP/EUR ticker cross without changing StrategyV2's closed-bar price, the 15-second anonymous spot trade/top-book snapshot, and closed five-minute spot/futures taker flow, volume, returns, relative strength and 10-minute OI change.
- Every live/5-minute layer carries its timestamp, age and `fresh` flag. Stale numeric fields become null. Prompt text explicitly distinguishes anonymous aggregate flow from trader identity, labels hourly CVD as a taker-volume proxy and settled funding as non-forecast data, and forbids a single micro spike from independently causing BUY or EXIT.
- Paid DeepSeek cadence remains five minutes and StrategyV2 thresholds/portfolios are unchanged. Release metadata advanced once to V4.3/code 58; package id remains `com.example.pumppaperbot.v8`. Static checks and focused timestamp/price-fallback tests were added; full Android CI and compatible final signing remain pending.
- Published the exact local V4.3 tree `bcdfd9a78ae0c1652ffe6da167ead429dec875e5` as remote commit `6343feb3201099b78af133b013f9d661e7522751` in draft PR #31. GitHub Actions run #127 (`30708109693`) completed successfully: unit tests, Kotlin/Android compilation, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.3/code 58, launch activity, intermediate signature, archive integrity and size checks passed.
- Artifact `PumpSignal-V4.3-Intermediate-APK` ID `8820996865` has archive SHA-256 `75dcf359f276d0ed0b2e939d0fd577f9dbb8205d27e975c7134aba419f31332b`; the contained intermediate APK is 7,031,412 bytes.
- Re-signed the CI APK with the installed-compatible update key. Final `PumpSignal-V4.3-Compatible-FINAL.apk` is 7,039,080 bytes with SHA-256 `729234de49a54f0c9994d633dcbe8500641539f66d58d02d6c03186f1d2d0f33`; certificate SHA-256 remains `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, v2/v3 verify, and `AndroidManifest.xml` plus all three DEX entries exactly match the CI artifact.

## V4.4 — reliable structured DeepSeek responses and diagnostics

- Serge's V4.3 API-center screenshot showed nine DeepSeek requests with five successful results and four errors. The visible failures were an unterminated JSON string and an empty Pro content field; code audit confirmed both clients parsed `message.content` immediately without checking `finish_reason` or preserving failed-response usage.
- Added one shared structured-response client for the primary-market and open-position circuits. It validates the outer envelope, `choices[0]`, `finish_reason=stop`, non-empty content and JSON integrity before a signal can be accepted.
- A structural interruption (`length`, empty content, malformed JSON or temporary insufficient server resource) permits exactly one repair call using the same current market frame, thinking disabled and a 550-token cap. HTTP errors and content-filter stops are not retried automatically. Repair attempts are explicit API journal events.
- Flash now requests low reasoning with a 1,600-token ceiling instead of the old medium/high-equivalent 1,200-token setup. Pro uses high reasoning and a 3,200-token ceiling; only an explicitly critical fallback retains max reasoning. These are ceilings, while billing remains based on actual usage.
- Failed-response token usage and finish reason now reach telemetry. The API center shows repairs and a conservative USD estimate using official cache-miss prices. Signals older than 12 minutes are marked stale and removed from the live DeepSeek gauge.
- Added a redacted in-app share report containing version, scheduler state, market-layer ages, token/cost totals and recent API events. API keys and full request payloads are excluded.
- Release metadata advanced once to V4.4/code 59; package id and stored state formats remain compatible with V4.3. Focused unit tests cover complete, truncated, empty, malformed and filtered responses, retry policy, cost rates and stale-signal timing. Full Android CI and compatible final signing remain pending.
- Published the exact local V4.4 tree `c0b8a16b7111c547430165130f5d6938133f407c` as remote commit `bff4a66840de018814894ea6d66f15b079e0adea` in draft PR #31. GitHub Actions run #129 initially stopped before Kotlin compilation because Maven Central returned HTTP 429 for standard Gradle dependencies; rerunning the unchanged job completed successfully, including all unit tests, Android compilation, package/version/activity checks, APK signature, integrity and size checks.
- Artifact `PumpSignal-V4.4-Intermediate-APK` ID `8821650421` has archive SHA-256 `d70455c43e0632b89496747385141f82c7c073b6fcef20e3a4627d3fc8340c02`; the contained intermediate APK is 7,042,620 bytes with SHA-256 `11c732856061de4571368953889da164365add2d9b25b1975ef0cfe769f0916c`.
- Re-signed the verified CI APK with the installed-compatible update key. Final `PumpSignal-V4.4-Compatible-FINAL.apk` is 7,051,368 bytes with SHA-256 `105d343bc378107c25e0344100731a1e7f8d3ab68cd8f34813d4e7ac30e4d01f`; certificate SHA-256 remains `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, v2/v3 verify, and `AndroidManifest.xml` plus all three DEX entries exactly match the CI artifact.

## V4.5 — copyable diagnostics and version-separated telemetry

- Serge's V4.4 screenshot was difficult to read and appeared dominated by errors. Time-order review showed that the API journal is newest-first: the lower 18:02–18:49 empty/truncated responses were preserved pre-update history, while fresh automatic Flash responses at 19:03 and 19:37 plus the 19:40–19:41 manual/API checks succeeded. No evidence justified changing the now-working DeepSeek response logic again.
- Added a dedicated «Скопировать диагностику» action that writes the complete redacted report to Android's clipboard and confirms that it is ready to paste into ChatGPT. The existing share-sheet action remains available.
- API status, usage and log panels are selectable. The log now explains its reverse chronology and labels each event with the producing app version; older persisted records without version metadata are explicitly marked as old-version history.
- Current-version request, success, error, repair, token and cost figures are separated from same-day events left by older versions. The diagnostic report includes both all-version and current-version totals for auditability.
- Release metadata advanced once to V4.5/code 60; package id and all stored trading/key state remain compatible. Full Android CI, APK inspection and compatible final signing remain pending.
- Published the exact local V4.5 tree `7f057f599b0234465f6fde155bc75f0e5e9efa1b` as remote commit `fb942bed833eabb48c509f65f61d6a10dcaaa1c1` in draft PR #31. GitHub Actions run #131 (`30711736007`) completed successfully, including unit tests, Kotlin/Android compilation, package `com.example.pumppaperbot.v8`, V4.5/code 60, launch activity, intermediate signature, archive integrity and size checks.
- Artifact `PumpSignal-V4.5-Intermediate-APK` ID `8822103951` has archive SHA-256 `dfc7f7987ce70cc3baa1887c59d5ebe80db109f53951bae7887c7b60f296397f`; the contained intermediate APK is 7,044,600 bytes with SHA-256 `dec53c22346ceab8ba6e775f525a277bf550d8a018903ac7a608b904c2eebac2`.
- Re-signed the verified CI APK with the installed-compatible update key. Final `PumpSignal-V4.5-Compatible-FINAL.apk` is 7,051,368 bytes with SHA-256 `f13265c448f2c4db7da41e3036f887f4b996f2ad4fd62d559554597a938f6a1b`; certificate SHA-256 remains `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, v2/v3 verify, and `AndroidManifest.xml` plus all three DEX entries exactly match the CI artifact.

## Post-V4.5 — accumulated minor corrections

- Serge approved a standing workflow for minor, non-urgent corrections: record each agreed item in the repository backlog, retain it across chats, and combine compatible items into the next worthwhile release instead of creating an APK for every small change.
- The first open item comes from the V4.5 copied diagnostic report at 2026-08-01 20:15 +0200. The successful Pro request started at 20:14:48 and completed at 20:15:25, but `lastSuccess` displayed the start time. A future accumulated release must make `lastSuccess` represent successful completion/acceptance time while leaving `lastAttempt` as request-start time and leaving scheduling/trading behavior unchanged.
- Third accumulated change requested by Serge: extend the DeepSeek diagnostics pattern to Gemini so both provider centers expose equivalent logs, status information and one-tap copy/share controls while keeping provider telemetry clearly separated.
- For both DeepSeek and Gemini, a manual provider check must also execute an expanded self-diagnostic and log individual PASS/WARN/FAIL checks for connectivity/API path, selected model and response parsing, market-data freshness/availability, relevant circuit/scheduler state, telemetry persistence and other implemented runtime prerequisites.
- The redacted audit output must preserve the observable decision trail needed for review by another AI or developer: sanitized context summaries, executed stages, accepted action and model-supplied reasons/evidence, timestamps/durations, tokens, finish state, errors, retry/repair activity and self-diagnostic results. Private model chain-of-thought is neither available nor to be requested; API keys, authorization data and full sensitive payloads remain excluded.
- This is a future accumulated-release requirement only. It must not by itself alter trading thresholds, provider authority or paid automatic cadence, and this documentation update does not require an APK build.
- This repository documentation update intentionally changes no Android source, version metadata, API cadence or trading logic and therefore does not require a standalone APK build.

## V4.6 — provider diagnostics parity and Russian-only output

- Serge deliberately scoped the accumulated backlog as V4.6 and added a fourth requirement: DeepSeek and Gemini user-visible messages must be entirely Russian; Chinese ideographs must not appear in the application or copied diagnostic reports.
- Added equivalent copy/share diagnostics actions to both provider centers. The Gemini report is provider-specific and includes quota, hourly expert state, last accepted decision/reason/risks, news analysis, market freshness, per-version usage and recent API events; the DeepSeek report retains primary/position separation and now includes evidence and risks.
- A manual provider check now runs the real provider request followed by persisted self-diagnostics. API key/path, accepted model/structured response, market freshness, scheduler/circuit state, telemetry persistence, provider-specific runtime prerequisites and visible language are reported individually as PASS/WARN/FAIL.
- Added a Russian-output policy that detects Han script. Prompts explicitly require Russian-only visible fields; DeepSeek treats a language violation as a repairable schema failure, Gemini rejects the response with a Russian error, and legacy saved state/log text containing Han characters is replaced on read with a Russian explanatory placeholder.
- DeepSeek primary and position `lastSuccess` now use the actual completion/acceptance timestamp while `lastAttempt` remains the scheduling start timestamp. Successful telemetry also records finish state, accepted action, observable reason/evidence/risks and sanitized input context summaries.
- Release metadata advanced once to V4.6/code 61. Package id, provider cadence, trading authority, strategy thresholds, portfolios and stored credentials remain unchanged. Focused language-policy tests and full Android CI/APK/signature verification are required before delivery.
- Published exact V4.6 tree `f7a6f70ddb90dbd08ab75fe4c774256b5f2e3c8a` as remote commit `02c0f6ac295e9c62fcd861beae7328c8956b0450` in draft PR #31. GitHub Actions run #136 (`30714886451`) passed unit tests, Kotlin/Android compilation, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.6/code 61, launch activity, intermediate v2 signature, archive integrity and size checks.
- Artifact `PumpSignal-V4.6-Intermediate-APK` ID `8823051022` has archive SHA-256 `9a01f88b281677b2cc0f00e558fb0844237e2c18f6b4534f842c8a581827208b`; the contained intermediate APK is 7,059,944 bytes with SHA-256 `25268427792ecbdd84f6450f8bfd1344a2c8b16e3fd4bdea439b6c90d22c86c8`.
- Re-signed the verified CI APK with the installed-compatible personal update key. Final `PumpSignal-V4.6-Compatible-FINAL.apk` is 7,141,679 bytes with SHA-256 `169e480977c3c59d82673cd692443b33e44896e932b3b610e4a29dd1dfbe01c5`; certificate SHA-256 remains `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, v2/v3 verify, and `AndroidManifest.xml` plus all four DEX entries exactly match the CI artifact.

## V4.7 — DeepSeek owns AI paper trading

- Serge clarified that V4.6 only labelled DeepSeek as primary while the actual paper-trading authority and both AI portfolios remained wired to Gemini. This was the architectural cause of DeepSeek being unable to act on a detected move.
- The primary DeepSeek market response now owns the former Gemini virtual account. Every accepted response is processed exactly once; `BUY` and `EXIT` execute against a fresh post-response PUMP/EUR quote, while `HOLD/WATCH` record a non-trading decision. Existing V4.6 balance, open position and history are preserved through the legacy store keys.
- A one-time ownership watermark is written before the first V4.7 DeepSeek request. It deliberately marks the last V4.6 advisory response as already observed, preventing installation from retroactively trading an old signal; the first response created after activation is eligible normally.
- The same one-time migration retires any unexecuted pending Gemini response so it cannot fire after the ownership transfer. It does not reset or rewrite the virtual balance, open position, completed trades, decisions or performance history.
- DeepSeek's prompt and action validation now use the DeepSeek paper position, not Serge's manual position. Outside the AI position it may return `BUY/HOLD/WATCH`; inside it may return `HOLD/WATCH/EXIT`. It cannot change APP or Serge.
- The former Gemini experiment is relabelled and rewired as DeepSeek experiment without resetting its portfolio. It mirrors a main DeepSeek BUY or uses a fresh positive DeepSeek/APP anchor with the established buyer-flow, CVD and safety vetoes; its independent market-evidence exit remains intact.
- Removed all automatic Gemini hourly trading and automatic Gemini news interpretation from foreground and reserve cycles. Gemini remains available only through an explicit manual API/news check as a non-trading second opinion.
- Updated account screens, comparison charts, trade attribution and loud trade notifications to DeepSeek ownership. Release metadata advanced once to V4.7/code 62; package id and compatible update line remain unchanged.
- Published exact V4.7 tree `283d9566dc9a892af13294ac887b80e49a83f745` as remote commit `d241ceacd4727bacc1c48cfebdbd450cd404fa91` in draft PR #31. GitHub Actions run #138 (`30734618743`) passed unit tests, Kotlin/Android compilation, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.7/code 62, launch activity, intermediate v2 signature, archive integrity and size checks.
- Artifact `PumpSignal-V4.7-Intermediate-APK` ID `8829138152` has archive SHA-256 `bc89fd7c731b6a4e5fcd9cf956a2f96896c17c80e51b170e577a52a2cafdafe3`; the contained intermediate APK is 7,062,508 bytes with SHA-256 `12d7f391096cfdc81aabd2e0d77c1f55dbb5551115b593bf3f77bd1f50a71d61`.
- Re-signed the verified CI APK with the installed-compatible update key. Final `PumpSignal-V4.7-Compatible-FINAL.apk` is 7,145,775 bytes with SHA-256 `fd711c9f11507dcae55b59a24ded4df6acd3064635e68267da2ac60211b18b6b`; certificate SHA-256 remains `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, v2/v3 verify, and `AndroidManifest.xml` plus all four DEX entries exactly match the CI artifact.

## V4.8 — higher-value DeepSeek cadence and trade verification

- Serge's DeepSeek platform showed 245 requests and 584,745 tokens for $0.09. At the observed average of roughly $0.00037 per request, the five-minute cadence costs only about $0.10/day; Serge explicitly allowed increasing useful analysis to $0.20–$0.50/day.
- The primary paid cadence is shortened from five minutes to two minutes, matching the existing foreground market-sync cycle. This projects approximately $0.26–$0.30/day at the observed request shape while giving DeepSeek updated live price, book and 15-second micro-flow data inside a still-open 30-minute candle.
- A proposed BUY or EXIT now requires a second adversarial DeepSeek Flash call with high reasoning. It independently rechecks freshness, late-entry/overheat, rapid drop, spot/futures alignment and BTC/SOL context. Rejection, malformed output or API failure safely converts the trade into WATCH/HOLD.
- The primary prompt now explicitly permits confirmed intrabar entry only when sustained micro-flow CONFIRMATION has an independent fresh five-minute or higher-timeframe confirmation and no late/overheat or broad-market veto. A single micro spike remains insufficient.
- Added a persisted conservative cache-miss cost estimate to the daily primary state and a $0.50 UTC-day circuit breaker. Verification token usage contributes to the same ceiling. Existing DeepSeek portfolios, trade ownership, keys and history remain unchanged.
- The budget counter is provider-wide rather than journal-window based, so primary analysis, trade verification, repairs and Serge-position supervision all contribute even after old detail rows rotate out. The three-minute sustained micro-confirmation window now safely overlaps the two-minute paid cadence; a rejected BUY is neutralized so it cannot become an experiment entry anchor.
- Release metadata advanced once to V4.8/code 63. Focused policy/cost tests, Android CI, direct APK inspection and compatible final signing remain pending.
- Published exact V4.8 tree `5b021744d0dedb873537191a68409aea749acc8c` as remote commit `319acea4c2b9ba3bf7e0fcbc68eee6e22dc22647` in draft PR #31. GitHub Actions run #140 (`30738352628`) passed unit tests, Kotlin/Android compilation, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.8/code 63, launch activity, intermediate v2 signature, archive integrity and size checks.
- Artifact `PumpSignal-V4.8-Intermediate-APK` ID `8830424686` has archive SHA-256 `4d5f82ece94462374b8214c2632b539a1aba317cd5f8aebfb54260d9d0216bcd`; the contained intermediate APK is 7,069,288 bytes with SHA-256 `a8b2b8740268db70b965ec93c00e2b2e2b46de11b3f832b1a55550f12e4d7c68`.
- Re-signed the verified CI APK with the installed-compatible update key. Final `PumpSignal-V4.8-Compatible-FINAL.apk` is 7,075,944 bytes with SHA-256 `038c7af6b5c3321518cd67a85aee2c06f523685d14d405caafd927c3f26d68e3`; certificate SHA-256 remains `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, v2/v3 verify, and `AndroidManifest.xml`, all four DEX entries and `resources.arsc` exactly match the CI artifact.

## V4.9 — execution freshness and active position protection (in progress)

- Started from the exact verified V4.8 source tree. No remote branch, commit, release or APK has been published yet.
- Introduced one paper-execution policy: BUY/EXIT decisions expire after 12 minutes, live quotes expire after five minutes, and simulated fills use the same conservative 0.05% adverse slippage in APP, primary DeepSeek and the DeepSeek experiment. Mark-to-market remains based on the raw quote.
- Manual `Я купил` now requires and records a fresh intrabar execution price instead of the last closed 30-minute candle. Manual sale, position P/L, chart and account display also prefer a fresh live price.
- Added three independent, persisted entry-reminder sources (APP, primary DeepSeek and DeepSeek experiment). They repeat at six-minute intervals within a bounded 20-minute signal window and stop after Serge confirms a purchase or the market has chased too far.
- BUY notifications from virtual competitors are muted while Serge's real position is open, but their virtual executions, histories and annual competition continue unchanged. Fresh V4.9 alarm-channel IDs avoid inheriting a previously muted Android channel.
- Fixed generic APP entry-alert delivery so notification permission/channel availability is checked before a signal is marked as delivered; failed delivery can therefore retry instead of silently disappearing.
- Added a free local manual-position guard using fresh live price, peak drawdown, rapid-drop, micro-flow and broad-market evidence. It can sound an immediate repeated exit warning and escalate DeepSeek/Gemini checks without waiting for a 30-minute candle or consuming an API request itself.
- The APP strategy thresholds were deliberately not loosened without new out-of-sample evidence. Its independent saved decision journal already exposes the exact missing entry condition. The real reliability defects were fixed instead: background APP signals now remain executable for one 15-minute Android reserve cycle (maximum 20 minutes), require a fresh quote, and are rejected if that quote has already chased more than +1.5% above the decision price.
- DeepSeek supervision of Serge's open position now runs every five minutes normally and every two minutes after critical evidence. Its saved state has primary/backup recovery and a durable pending-notification flag, so Android permission/channel failure no longer falsely marks an EXIT alarm as delivered. Recent RSS headlines and explicit Fed/US-president/Trump/BTC/SOL/PUMP macro-risk instructions were added to its position frame.
- Added a quota-aware Gemini position advisor with no trading authority. It runs immediately after `Я купил`, every 90 minutes normally and every 15 minutes during danger, within the existing shared 12/25 daily request reserve. It checks live price, P&L/peak pullback, book, spread, OI/funding, 15-second/5-minute flows, PUMP/BTC/SOL and untrusted RSS, and uses Google Search for fresh Fed/Trump/macro/crypto context when supported. A rejected Search tool is retried without Search and paused for 24 hours to avoid wasting the daily quota.
- The DeepSeek experiment now incorporates fresh 15-second buyer-flow weakness in its exit evidence. At +8% or more it can immediately protect a pullback of at least 0.8% when live flow and another independent market group agree; from +4% it enters the existing two-check confirmation path sooner. The emergency loss and anti-single-tick protections remain.
- Updated the Gemini center and redacted diagnostics to show manual market/news checks separately from automatic, non-trading supervision of Serge's position. Added a Gradle 8.5 Wrapper and made unit tests, Android lint and APK assembly mandatory in CI.
- Release metadata advanced once to V4.9/code 64 while retaining package `com.example.pumppaperbot.v8`. Local verification passed 124 tests across 28 suites (0 failures, 0 errors, one intentionally skipped optional external historical-fixture research test), Android lint with 0 errors, `assembleDebug`, APK ZIP/signature verification, launch metadata and package/version inspection. The local APK is an intermediate Android-debug-signed artifact only and must not be offered as the compatible update.
- Published the verified V4.9 implementation as commit `29ae44976d3f93c923fb8a6dd1f67fad715592d2` on branch `agent/v4-9-execution-entry-supervision` and opened draft PR #32. GitHub Actions run #141 (`30743058680`) passed wrapper-based unit tests, Android lint, assembly, package `com.example.pumppaperbot.v8`, V4.9/code 64, launch activity, intermediate v2 signature, ZIP integrity and size checks.
- Artifact `PumpSignal-V4.9-Intermediate-APK` ID `8831976616` has archive SHA-256 `c7dec97f8e5c9f6f7f3a2512b6527b72b18b4c4d889f48035750ab47a3f5ddce`; the contained intermediate APK is 7,101,144 bytes with SHA-256 `0a85b4188948cfb9e6828ecd65277dbe807246f3a2aa33f52812fe0659ca8af2` and four DEX files. It is signed by the ephemeral GitHub Android-debug certificate, not the compatible certificate ending `27dc7823`; compatible final signing remains deliberately pending until the personal update key is available in an authorized signing environment.

## Post-V4.9 — maximum support for Serge's open position

- Serge clarified the resource policy for the next accumulated release: pressing `Я купил` must switch the application into its highest-priority personal-position protection mode until that position is closed. Supporting the real position takes precedence over routine market analysis, virtual-account research and saving quota/cost for a possible later trade or the next day.
- DeepSeek personal-position supervision must be exempt from the ordinary provider-wide `$0.50` daily safety ceiling that governs lower-priority automatic analysis. Gemini's position advisor may consume all of the day's remaining shared request allowance, including quota that would otherwise be reserved for manual/routine analysis. When capacity is constrained, lower-priority calls yield to position protection.
- This permission is to maximize useful coverage over the full life of the position, not to generate duplicate requests without new evidence. Run both advisers immediately after entry, keep supervision aligned with the freshest useful market cycle, escalate on new local danger and continue until Serge records a sale or the provider's hard limit is genuinely exhausted. Do not stop or slow a useful position check merely to conserve resources for another trade: Serge normally enters no more than once per day and explicitly accepts exhausting the remaining daily AI resources on that trade.
- Hard external rate/quota limits, missing credentials/network and response-integrity safeguards remain real boundaries. If either provider is unavailable or exhausted, the free local position guard, fresh-price tracking and urgent Android alarms must continue independently. This documentation-only update changes no V4.9 APK, runtime cadence, version metadata or stored state; implementation and focused budget/priority tests belong to the next deliberately scoped release.

## 2026-08-02 — repository consolidation and shared-agent workflow

- Agent: ChatGPT Work (Codex-compatible repository maintenance). Reason: Serge requested one understandable development line, mandatory shared memory for both assistants, removal of obsolete parallel versions and durable direct APK downloads.
- Updated root `AGENTS.md` with the mandatory two-file protocol: every agent must read `AGENTS.md` and `DEVELOPMENT_LOG.md` before work; `main` is the sole canonical line; agents must reuse an active task branch, merge verified work, delete finished temporary branches, record every material action, never commit APK/Base64 build chunks, and provide verified local plus GitHub Release asset links.
- Preserved the post-V4.9 product rule in both coordination records: after `Я купил`, Serge's real open position receives maximum-priority DeepSeek/Gemini support and may consume the remaining daily provider allowance; local protection and urgent alarms remain independent fallbacks.
- Promoted the verified source history in order: PR #31 (V4.8) merged to `main` as `b210bde5d5e3015a2a29165cea552d50df1c0524`; PR #32 (V4.9 plus the coordination updates) merged to `main` as `b0c6ff0011298788b9d3ae4a61ae56f115770b69`. The canonical tree now reports package `com.example.pumppaperbot.v8`, `versionName 4.9` and `versionCode 64`.
- Closed superseded open PRs #23–#28 after confirming that the current V4.9 line is canonical. PRs #31 and #32 are closed by successful merge. No Android runtime source was changed by this maintenance task beyond the already verified V4.9 implementation.
- Recovered the two retained compatible APKs. V4.8 is 7,075,944 bytes, SHA-256 `038c7af6b5c3321518cd67a85aee2c06f523685d14d405caafd927c3f26d68e3`; V4.9 is 7,108,712 bytes, SHA-256 `46591dd1e05ba88b5f02cd8359052e47b24b63f8b7c0e5869e4b3022d00a37d0`. Both hashes match their previously verified compatible-final records.
- Remaining repository-cleanup step: create/preserve immutable V4.8 and V4.9 release points, upload each compatible final APK as a direct GitHub Release asset, then delete the eleven redundant remote branches (ten finished agent branches plus `release-v4.9-apk`, whose only unique content is the obsolete 14-part Base64 copy). The connected GitHub API cannot perform release-asset upload or branch deletion; the GitHub web session was signed out, so these destructive/publishing steps remain explicitly pending rather than being claimed as complete.

## V4.10 — selectable fast chart and maximum personal-position priority (in progress)

- 2026-08-03, Codex Work Mode. Serge reported that the main 30-minute chart hides an already visible PUMP move and deliberately scoped the correction as V4.10. Work started from consolidated `origin/main` commit `6f1a4e4` on the single branch `agent/v4-10-chart-speed`; GitHub had no existing V4.10 branch or open PR.
- Added a large main-screen chart-speed control with 1m, 5m, 15m, 30m and 1h choices. The manual choice is persisted. Each new Serge entry selects 1m exactly once, including an already-open position first seen after upgrading; later manual changes for that same position are respected.
- Added a display-only PUMP/EUR candle client and cache for the selected interval. The open candle is updated from the existing 15-second WebSocket micro-price when fresh, short views receive time-aware axis labels, and EMA50/EMA200 are recalculated for the visible interval. StrategyV2, paper execution, RSI/EMA decisions and backtests remain wired to the original closed 30-minute market store.
- Implemented the previously recorded maximum-support policy. `PositionSupervisorClient` may continue paid DeepSeek checks past the ordinary $0.50 research ceiling while Serge's position is open; the lower-priority primary research circuit retains that ceiling. Gemini's position adviser explicitly receives the full remaining 25-request daily allowance, while ordinary/background Gemini requests retain the 12-request half quota so they cannot consume the position reserve.
- Added focused tests for the five intervals, one-time post-buy switch, live current-minute update/append, DeepSeek position-ceiling exemption and Gemini priority quota separation. Advanced release metadata to V4.10/code 65 and updated README plus CI package/artifact checks. Files: `ChartSpeed.kt`, `ChartMarketClient.kt`, `MainActivity.kt`, `StrategyChartView.kt`, `PositionSupervisor.kt`, `GeminiRequestBudget.kt`, `GeminiPositionAdvisor.kt`, layout/tests/build/workflow/docs.
- Verification so far: `git diff --check` passed and `activity_main.xml` parsed successfully. The first local Gradle attempt could not start because the managed workspace blocks writes to `/root/.gradle` and no Gradle 8.5 distribution cache is present; this is an environment bootstrap failure, not a test result. Full unit tests, lint, Android assembly, direct APK inspection and compatible final signing remain required before release.
- Remote commit `1914e3ce517d3e3ea107651c1eecd96987e52432` opened draft PR #33. GitHub Actions run #148 compiled the complete Android application successfully and ran 130 tests; one old V4.1-era assertion failed because it expected every Gemini circuit to receive all 25 requests whenever a position was open. V4.10 intentionally reserves the full limit for `positionPriority=true` while routine/background requests remain capped at 12. Updated that stale test without changing runtime code; a clean rerun is required.
- Published the test correction as remote commit `512a31ede3a0f7f1652c981d8cbdac6a156574d1` in the same PR #33. GitHub Actions run #149 (`30836045341`) passed all required stages: Android/Kotlin compilation, 130 unit tests with zero failures (one intentionally skipped optional external historical-fixture research test), lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.10/code 65, launch activity, intermediate v2 signature, ZIP integrity and size checks.
- CI artifact `PumpSignal-V4.10-Intermediate-APK` ID `8864954376` has archive SHA-256 `95ff8566ec7aa6473a620baaba68d07ac563fe274d9f418be8d38b3e433c3172`; the contained intermediate APK is 7,113,248 bytes with SHA-256 `ed85dd973618ee5dd3bf63afe4a72fc9db8b7f512450a1794d3c421961b6dd71`.
- Re-signed that exact verified APK with the installed-compatible personal update key. Final `PumpSignal-V4.10-Compatible-FINAL.apk` is 7,194,927 bytes with SHA-256 `9af4f5ee400525b5b0051bbe22c5c92dd1b52bb2d3000d3ebf9e139ea9a2a67e`. APK Signature Schemes v2/v3 verify; certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.9. `AndroidManifest.xml`, `resources.arsc` and all four DEX entries exactly match the CI-verified intermediate APK. Merge to `main`, immutable tag/Release and durable Release asset remain pending.
- Marked PR #33 ready only after the verified head CI passed, then merged it into canonical `main` as `dea76d6c32227e9dea039d8dc9faacc7e7b49086`. Source integration is complete. Remaining release operation: create immutable tag `v4.10`, publish GitHub Release `PumpSignal V4.10`, and upload the one final compatible asset; the connected GitHub API available in this workspace has no release/tag-asset operation, so this is recorded as pending rather than falsely claimed.

## V4.11 — profit-adaptive DeepSeek position support (in progress)

- 2026-08-03, Codex Work Mode. Serge reported feeling under-supported while his real PUMP position was roughly +4–5% because DeepSeek refreshed too rarely and gave too little detail about the order book, executed buying/selling pressure and Bitcoin. He deliberately scoped the improvement as V4.11 and accepted approximately €1–2 of provider cost when useful position support can protect a roughly €1,000 trade.
- Started from canonical `main` commit `3797bf2` on the single local branch `agent/v4-11-profit-adaptive-position-support`; no local or tracked remote V4.11 branch existed. Existing compatible package id and saved stores are retained.
- Added an explicit support plan. Below +2% the position adviser uses Flash at a three-minute cadence while the free foreground market cycle stays at two minutes. At +2% current or already reached peak profit, the full foreground sync and DeepSeek Pro position analysis target one minute. At +4%, on a profitable-peak pullback confirmed by fresh seller flow, or on existing critical evidence, Pro uses maximum reasoning at the same one-minute cadence. The existing single-flight guards prevent overlapping paid calls when a response lasts longer than the cycle.
- Expanded the free WebSocket observer without changing trading authority. It now retains PUMP aggressive-buy ratios for 5s/15s/60s/5m, 60-second aggressive buy/sell notional, and a separate BTC aggregate-trade stream with 15s/60s buyer ratios and 60-second price change. The REST market sync persists total bid and ask notional across the existing 20-level PUMP depth snapshot in addition to imbalance/spread.
- Expanded the DeepSeek position frame and structured response. The model receives current/peak P&L and pullback, support tier, 20-level bid/ask totals, richer PUMP micro-flow and live BTC micro-flow. It must return separate Russian fields for decision summary, book state, executed-flow state, Bitcoin effect and a concrete watch/invalidation condition. The prompt explicitly treats one book wall as potentially spoofable and requires confirmation by executions and price.
- The main position card now displays support tier, current and peak profit, pullback, decision/danger, book, executed trades, Bitcoin and what to watch. The general microstructure panel also displays live PUMP 60s/5m buyer shares and BTC 60s change/buyer share.
- Advanced metadata once to V4.11/code 66 and updated README and CI artifact/package checks. Added focused tests for +2% one-minute Pro escalation, +4% max reasoning, seller-confirmed profitable pullback escalation, routine Flash cadence and persistence of the richer position state.
- Verification so far: `git diff --check` and Android XML parsing passed. Local Gradle could not start because the managed workspace cannot reach `services.gradle.org` and has no complete cached Gradle 8.5 distribution; this is an environment bootstrap failure, not a Kotlin/test result. Full unit tests, lint, Android assembly, direct final-APK inspection and compatible certificate verification remain required.
- Created local implementation commit `d679587` on `agent/v4-11-profit-adaptive-position-support`; the worktree was clean immediately afterward. A direct push to `origin` was rejected by the managed publication guard because this turn did not contain fresh explicit authorization to disclose the new V4.11 source to `rosergenbg-debug/pump-paper-bot-android`. No remote V4.11 branch, PR, CI run or APK has been claimed. Next step is to obtain Serge's explicit permission for that exact repository, push this one branch, and run the mandatory clean GitHub Actions verification.
- 2026-08-03, Serge explicitly authorized publishing the already prepared V4.11 source to the public repository, opening its PR and running GitHub Actions. The local repository has no authenticated `gh` session, so publication uses the connected GitHub app with the same bounded branch and diff; final CI facts, APK hashes/signature and merge status must be appended only after they actually complete.
- Published the exact authorized V4.11 tree through the connected GitHub app as remote commit `9bde35189ab8f55fa006edfc983aa81007e3407b` on `agent/v4-11-profit-adaptive-position-support` and opened draft PR #34. GitHub Actions run #153 (`30839894537`) completed successfully in 3m19s: Kotlin/Android compilation, the full unit-test task, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.11/code 66, launch activity, intermediate v2 signature, ZIP integrity and size checks all passed.
- CI artifact `PumpSignal-V4.11-Intermediate-APK` ID `8866423948` is 6,149,759 bytes with archive SHA-256 `033f07911402518eacb18eb0c3ce6ac932c9b7b1246df2a7b196aa718e641a48`. The contained intermediate APK is 7,125,324 bytes with SHA-256 `c08d1e06e21a599a1ebe2b74787de948fdba22a576b7fe44a9cc814ab46c0044`.
- Re-signed that verified APK with the installed-compatible personal update key. Final `PumpSignal-V4.11-Compatible-FINAL.apk` is 7,133,288 bytes with SHA-256 `ebe7835823b4444b03a803b968acb30617ce3aaa7541836548c6ab53483217f9`; APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.10. `AndroidManifest.xml`, `resources.arsc` and all four DEX entries are byte-identical to the CI-verified intermediate APK. Notification permission, high-importance alarm channels and alarm sound remain present. PR merge, immutable tag/Release and durable Release asset remain pending.
- The documentation-only head commit passed a second complete GitHub Actions run #154 (`30840510351`). Its independently rebuilt APK had the same `AndroidManifest.xml`, `resources.arsc` and four DEX hashes as run #153. Marked PR #34 ready after that check and merged it into canonical `main` as `3b89641a8f5c086d902a86448ca4552530a2b475`. Source integration is complete; immutable `v4.11` tag, GitHub Release and durable Release asset remain pending because the connected GitHub toolset exposes no tag/release-asset operation.

## V4.12 — immediate comparison trade markers (in progress)

- 2026-08-03, Codex Work Mode. Serge reported that after pressing the manual exit button the four-account comparison could still omit the fresh Serge `ВЫХОД` marker for 15–20 minutes even though the account had already changed internally. Investigation confirmed that `CompetitionActivity` re-rendered every five seconds and read the updated user portfolio, but `CompetitionChartView` filtered markers to the time range ending at the last closed 30-minute strategy candle. A trade inside the open half-hour therefore remained beyond the chart boundary until that candle closed.
- Started the bounded correction from canonical `main` commit `1746af7` on `agent/v4-12-instant-competition-exit`. The comparison now uses the same display-only 30-minute live-edge policy already proven by the selectable chart and prefers the fresh visible price for mark-to-market. A new current candle makes fresh BUY/SELL timestamps eligible on the next five-second screen render; StrategyV2 and all stored closed-candle decisions remain unchanged.
- Added focused tests for appending the current 30-minute display candle and updating an existing open display candle. Advanced release metadata once to V4.12/code 67 and updated README, CI artifact/package checks and the durable version chain. Local unit tests, lint, Android assembly, direct APK inspection, compatible signing and publication remain required.
- Static verification passed `git diff --check`, all Android resource XML files parsed successfully and no stale V4.11 runtime/workflow metadata remains outside retained history. The local Gradle run stopped before compilation because Gradle 8.5 is not cached and the managed workspace cannot reach `services.gradle.org`; this is an environment bootstrap failure, not a test result. Clean GitHub Actions compilation remains required before any APK is offered.
- Created local implementation checkpoint `c92ca63` on `agent/v4-12-instant-competition-exit`; the worktree contained only the eight intended V4.12 code/test/version/documentation files and was clean after the commit. The connected GitHub app can publish and run CI, but no V4.12 source has been sent externally yet because this new public branch requires Serge's explicit authorization.
- 2026-08-03, Serge explicitly authorized publication of V4.12 and granted standing authorization for later PumpSignal versions in this same public repository: publish bounded source branches, open/update PRs and run GitHub Actions without requesting a fresh per-version confirmation. The standing authorization does not broaden the repository scope or remove signing, destructive-cleanup, credential and release-verification safeguards. V4.12 publication may now proceed through the connected GitHub app because this workspace has no `gh` executable.
- Published the exact authorized V4.12 tree through the connected GitHub app as remote commit `06cd6caf1e21f4bcf1b7e9095ad4af726d362230` on `agent/v4-12-instant-competition-exit` and opened draft PR #35. GitHub Actions run #157 (`30842468710`) passed all required stages: Kotlin/Android compilation, the full unit-test task including the two new live-edge cases, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.12/code 67, launch activity, intermediate v2 signature, ZIP integrity and artifact upload.
- CI artifact `PumpSignal-V4.12-Intermediate-APK` ID `8867440945` is 6,149,991 bytes with archive SHA-256 `174ce5912e6d22145258f0d35169ba007f93d4c6bf91fed844adfb505b2348a4`. The contained intermediate APK is 7,125,740 bytes.
- Re-signed that verified APK with the installed-compatible update key. Final `PumpSignal-V4.12-Compatible-FINAL.apk` is 7,133,288 bytes with SHA-256 `30752cfe3c39e52454572b165cb13151edb640e3db2c25928dfabf9410d235fa`; APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.11. `AndroidManifest.xml`, `resources.arsc` and all four DEX entries are byte-identical to the CI-verified intermediate APK. A documentation-only head commit and clean repeat CI remain required before merge to `main`.
- The documentation-only head commit passed a second complete GitHub Actions run #158 (`30843012174`). Its independently rebuilt APK had the same `AndroidManifest.xml`, `resources.arsc` and four DEX hashes as run #157 and the signed final file. Marked PR #35 ready and merged it into canonical `main` as `d0924d61a512db9a8efdd2a59b0fde10b46720d4`. Source integration is complete; immutable tag/Release and durable compatible APK asset remain the only release-publication items not yet completed.

## V4.13 — adaptive DeepSeek action scale and explicit trade alerts

- 2026-08-04, Codex Work Mode. Serge requested unmistakable entry and exit notifications from every virtual participant, a repeated entry alert when he has not pressed `Я купил`, and a larger DeepSeek `1–10` indicator that changes from red/yellow/green before entry to green/yellow/red after entry. He also requested faster DeepSeek processing when live order flow begins moving.
- Started from the completed local V4.12 tree on `agent/v4-13-deepseek-action-readiness`. Added a conservative user-action fusion layer that combines the fresh DeepSeek structured result, APP readiness, PUMP micro executions and live Bitcoin flow. Late-entry, broad-overheat and unrecovered rapid-drop vetoes force entry level `1/10`; reaching green requires independent agreement and never changes StrategyV2 or automatically changes Serge's account.
- Added optional structured `entry_readiness 1..10` to the primary DeepSeek response and persisted it backward-compatibly. Yellow entry conditions shorten the foreground and paid primary cadence to one minute; confirmed green prefers DeepSeek Pro. Existing single-flight guards and the primary $0.50 research ceiling remain in force, while V4.11's separate open-position priority remains unchanged.
- Added a large first-screen card. Before purchase, levels `1–4/5–7/8–10` render red/yellow/green as do-not-enter/prepare/check-entry. After purchase the card uses the position supervisor plus local protection and reverses the colors to green/yellow/red for hold/prepare-exit/check-exit.
- Added separate quiet-hour-aware DeepSeek preparation/ready alerts on upward threshold crossings. Existing durable virtual trade outboxes remain the source of actual APP/DeepSeek/experiment entry and exit alarms; an explicit shared policy now proves that a SELL or partial SELL is never suppressed while Serge's position is open, while redundant BUY alarms remain muted then. Existing bounded entry reminders remain intact.
- Advanced release metadata once to V4.13/code 68 and updated README, CI artifact/package checks and durable project memory. Added focused pure unit coverage for hard vetoes, independent-confirmation caps, strong green entry, weak-Bitcoin cap, reversed exit colors, alert crossings and always-on virtual exits. Full unit tests, lint, Android assembly, direct APK inspection, compatible signing, publication and merge remain required.
- Static verification passed `git diff --check` and parsed every Android resource XML file successfully. The local Gradle wrapper could not download Gradle 8.5 because this managed workspace cannot reach `services.gradle.org`; this is an environment bootstrap failure before Kotlin compilation, not a test result. Clean GitHub Actions compilation remains mandatory before any APK is offered.
- Published the exact 16-file V4.13 checkpoint as remote commit `14b5393330e0d08c38f068ec0b9a16f057bc79e9` and opened draft PR #36. GitHub Actions run #161 reached Kotlin compilation and found one type error: the first UI bridge attempted to read transient `forceCriticalAi` from persisted `PersonalPositionGuardState`, although that flag belongs to the current `PersonalPositionGuardOutcome`. Replaced it with the persisted recent-alert timestamp while retaining direct sell/rapid-drop/micro-flow evidence; strategy and notification execution are unchanged. A clean full rerun is required.
- Published the focused type correction as remote commit `30887b7f1f278318adae10d78a373683c7da3fad`. GitHub Actions run #162 (`30886379997`) passed every required stage: Kotlin/Android compilation, the complete unit-test task including the new V4.13 policy cases, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.13/code 68, launch activity, intermediate v2 signature, ZIP integrity and artifact upload.
- CI artifact `PumpSignal-V4.13-Intermediate-APK` ID `8883323680` is 6,163,716 bytes with archive SHA-256 `e3c89f65848ed85b207fa5a545ad6e3cdb499cb01db4513efbe93d1b3953bca5`. Its single intermediate APK is 7,138,160 bytes with SHA-256 `529bf581e8bf652247551ecd8c1acb7e28bc3b1b44946a4b4c936aacb11f557c`.
- Re-signed that exact verified APK with the installed-compatible update key. Final `PumpSignal-V4.13-Compatible-FINAL.apk` is 7,145,576 bytes with SHA-256 `8748e1d1a3f3712769cc3ae7872f3d50cfd86caa3be375f861ce2ddbf5701f52`; APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.12. `AndroidManifest.xml`, `resources.arsc` and all four DEX entries are byte-identical to run #162. Direct DEX inspection retains the separate APP, DeepSeek, experiment and position channels plus the new V4.13 action-level alert store. A clean CI on the documentation head and merge to `main` remain required.
- Documentation head `740d1eb7c5d012d2b678dcb93ef004960fa1c91b` passed the second complete GitHub Actions run #163 (`30886997646`). Its artifact archive SHA-256 is `5a8c4f145f66af413925517bfc5c11c8216322f9970d08596b57267451439332`; the independently rebuilt APK again matched the signed final file for `AndroidManifest.xml`, `resources.arsc` and all four DEX entries. Marked PR #36 ready and merged V4.13 into canonical `main` as `4b262934e3f387a0b90b5a858649a031f8134d74`. Source integration is complete; immutable tag/Release and durable compatible APK asset remain the only release-publication items not yet completed.

## V4.14 — uninterrupted DeepSeek and critical market overview (in progress)

- 2026-08-04 18:03 CEST, Codex Work Mode. Serge requested removal of the `$0.50` daily DeepSeek stop, retaining only an informational warning around €5 without interrupting analysis; a separate graphical critical overview; a closer first-screen market chart; and upper/lower separation of profitable and losing connectors in the four-account comparison.
- Started from the completed local V4.13 tree on `agent/v4-14-unlimited-critical-overview`. Removed both runtime cost-return paths from `DeepSeekPrimaryAnalyst.kt` and `PositionSupervisor.kt`. `DeepSeekDailyBudgetStore` remains as cost telemetry only; `DeepSeekCostWarningPolicy/Store` and a default-importance notification issue one non-blocking daily warning at an estimated `$5` provider cost, described to the user as approximately €5. Provider diagnostics and the API center explicitly report that cost no longer blocks requests.
- Added `CriticalOverview.kt` and `CriticalOverviewActivity.kt`. The display-only policy combines the V4.13 action level, local direction, 20-level order-book imbalance/notional, live PUMP executions, closed 5-minute spot/futures taker flow, live Bitcoin and open interest. It renders eight red/yellow/green factor bars plus short-lived two-second PUMP/book/BTC trend lines while the screen is open. Missing or stale inputs remain unavailable rather than being invented as neutral evidence; late-entry, overheat, unrecovered rapid-drop and position-danger vetoes cap the overall result conservatively. The screen reads existing stores and does not initiate paid or network requests.
- The first-screen `StrategyChartView` now starts at 60 visible bars instead of 120 without changing the selected interval or StrategyV2 data. `CompetitionChartView` routes profitable connectors through a lane above both trade points and losing connectors through a lane below both points. Added focused tests for the new cost-warning threshold, critical-overview green/red/veto/missing-data cases, 60-bar default and connector geometry.
- Advanced release metadata once to V4.14/code 69 and updated manifest, first-screen button, README, workflow checks/artifact names and durable rules. Static verification passed `git diff --check`, parsed every Android XML resource and confirmed that no active Kotlin source still contains the old budget-blocking symbols or messages. Local Gradle could not reach compilation because Gradle 8.5 is not cached and `services.gradle.org` is unavailable in this workspace; full GitHub Actions compilation, unit tests, lint, APK assembly, direct APK inspection and compatible signing remain required.
- Published the 24-file implementation as remote commit `0120f8d1dc6ff1f1c2b8a0c739c13f8afecb41b5` and opened draft PR #37. GitHub Actions run #166 (`30927562946`) reached `compileDebugKotlin` and found one nullable-call error in `CriticalOverviewPolicy.combinedFlowScore`: the no-data branch intentionally returns `null`, but the final clamp used a non-safe call. Changed only that clamp to `?.coerceIn(...)`, preserving unavailable data as unavailable; a complete rerun is required.
- GitHub Actions run #167 (`30927874733`) showed that the first correction had matched the earlier non-null action-score clamp, leaving the nullable combined-flow clamp unchanged. Restored `actionScore` to a required `Int` and applied the safe call explicitly inside `combinedFlowScore`; a clean full rerun remains required.
- Published the explicit two-site correction as remote commit `900fc1b19613a4d7349639213736e825b59b1de7`. GitHub Actions run #168 (`30928272483`) passed Kotlin/Android compilation, the complete unit-test task, lint, `assembleDebug`, package/version/activity/ZIP verification and artifact upload. Artifact `PumpSignal-V4.14-Intermediate-APK` ID `8900135274` is 6,181,078 bytes with archive SHA-256 `e1cf3d218e027306bd7ac31756462aea767c028234e522d7395822a69fccbc6f`; its single APK is 7,155,552 bytes with SHA-256 `90c232a6c6198d40fe4f03fa197fe1f406cfb2909614422a377b8879d5b25a2a`.
- Re-signed that exact CI APK with the installed-compatible update key. Final `PumpSignal-V4.14-Compatible-FINAL.apk` is 7,161,960 bytes with SHA-256 `263392592d74d465adba75cb84f40011dd2111cca7c8c0e8378d50a5d4f2e668`; APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.13. `AndroidManifest.xml`, `resources.arsc` and all four DEX entries are byte-identical to run #168. Direct DEX inspection contains the critical-overview implementation and no old budget-blocking symbols. A clean CI on this documentation head and merge to `main` remain required.

## V4.15 — robust live breathing and independent intrabar DeepSeek (in progress)

- 2026-08-05, Codex Work Mode. Serge requested a second cross-check of all interacting rules after the missed 4 August PUMP impulse: restore the inactive-looking normal DeepSeek, stop 30-minute APP rules from blocking it, retain the nervous experiment within a 15-point sensitivity gap, smooth the critical live view over useful horizons, retain only 24 hours of observations and apply the previously agreed APP winner-following correction.
- Root-cause review of V4.14 confirmed three interacting traps. The critical chart retained only up to 120 two-second points in activity memory and lost them when closed; the WebSocket retained raw trades for only five minutes. DeepSeek scheduling reacted materially only to closed-strategy readiness/BUY/SELL changes, while the second trade verifier could convert a proposed BUY to WATCH and zero its direction/confidence, hiding the rejected proposal. Separately, StrategyV2 still fully sold a TREND position on the first candle touching +8%.
- Added `LiveMarketBreathing.kt`. The foreground WebSocket now appends one public PUMP/BTC/book observation every 15 seconds to a local CSV, prunes observations older than 24 hours and computes robust instant/5m/15m/30m/1h/6h scores using median flow, price movement and directional persistence. Normal DeepSeek uses the stable composite; the faster experiment blend is hard-clamped to ±15 points around it.
- Added the breathing JSON to both DeepSeek decision stages. A 12-point stable breathing change can wake the paid circuit without a new closed 30-minute candle. APP late-entry/overheat remains APP context but is no longer a hard veto in the DeepSeek action scale, prompt, verifier or critical-overview cap; an unrecovered rapid drop remains a hard veto. The experiment may bypass the old late flag only when both 5m and 15m continuation are sustained.
- Persisted `proposedAction`, verification summary and execution status backward-compatibly in the primary DeepSeek state. Rejected BUY/EXIT direction and confidence stay visible for diagnosis, while the final action remains WATCH/HOLD and the experiment explicitly refuses a rejected proposal as an entry anchor.
- Replaced the experiment's one-condition `microWeak` OR with at least two raw weak micro conditions plus smoothed 5/15-minute deterioration. The experiment retains its earlier-entry and peak-protection architecture; only its noisy evidence input is stabilized.
- Changed APP TREND +8% handling: the first target-touch candle returns WAIT and records the new peak. A later candle exits only after crossing a 4% trailing level protected above +2%, or under the unchanged hard stop/time limit. Added focused tests for the first-candle hold and later confirmed pullback.
- Critical overview now separates the raw screen-local line from a persistent breathing chart for now/5m/15m/30m/1h/6h and displays proposed action, final action, verifier result and execution status. API center and copied diagnostics expose the same decision trail.
- Advanced metadata once to V4.15/code 70 and updated README/workflow/durable rules. Added analyzer tests for outlier suppression, the 15-point experiment cap and 24-hour retention, plus updated rejected-BUY visibility tests. `git diff --check` passes. Local Gradle cannot download Gradle 8.5 because `services.gradle.org` is unreachable in this managed workspace; clean GitHub Actions unit tests, lint, Kotlin/Android compilation, APK assembly and direct final verification remain required.
- Published the exact 19-file V4.15 implementation tree (`60e199e012d87209157677b99a1e89336bed6a3a`) as remote commit `fc0f22e2d377f04ab759ed195c6ac96f6450ed28` on `agent/v4-15-smoothed-market-breathing` and opened draft PR #38. GitHub Actions run #171 (`30982360612`) passed Kotlin/Android compilation, the full unit-test task, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.15/code 70, launch activity, intermediate signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V4.15-Intermediate-APK` ID `8920698853` has archive SHA-256 `25039301975baabeceddcf2d068d6e5a66e239c5a25924be2576bc401fca16b1`; its APK is 7,176,688 bytes. Re-signed that verified APK with the installed-compatible update key. Final `PumpSignal-V4.15-Compatible-FINAL.apk` verifies with APK Signature Schemes v2/v3 and certificate SHA-256 `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.14; final file SHA-256 is `c05fb864a2b3652c3d8d22f49bb901202b9b315adc1611d82f2dd04331a9eece`. A clean CI run on this documentation head and merge to `main` remain required.

## Post-V4.15 — accumulated DeepSeek evidence memory and clearer analysis

- 2026-08-05, Codex Work Mode. Serge requested a future internal DeepSeek memory layer, with no separate window and no immediate APK. It should accumulate compact text-like/structured market context, predictions, decisions and later outcomes for short and long analysis, initially limited to 50 MiB. Approaching the limit must ask whether to add another 50 MiB block or prune, and must summarize how much of the retained material was actually productive.
- The future implementation must distinguish the existing V4.15 24-hour live-breathing store from this evidence memory. The former smooths current public PUMP/BTC/book observations; the latter evaluates historical hypotheses and prediction outcomes across declared 15m/1h/3h/6h/24h horizons. It should retrieve only relevant compact evidence into a DeepSeek prompt rather than transmitting the entire archive.
- A claimed useful pattern must be scored from a prediction frozen before its outcome. Promotion into higher-weight analysis requires at least 30 independent evaluable occurrences, at least 60% directional precision for the declared horizon, positive expectancy after the existing round-trip fees, and measurable lift over baseline. Unproven material remains background context; promoted evidence is revalidated walk-forward, decays when stale and is demoted if performance deteriorates. Memory alone cannot execute a trade or bypass freshness and risk controls.
- Future DeepSeek output should retain machine-readable actions while adding a short Russian human explanation: current regime, the two or three strongest evidence groups, likely short/long scenario, invalidation condition and explicit uncertainty. This is an observable decision rationale, not private chain-of-thought. Routine Flash analysis remains economical; stronger Pro/max reasoning should be used selectively for conflicting evidence, proposed BUY/EXIT, regime change and Serge's protected open position.
- Documentation-only backlog change: no Android source, version metadata, cadence, trading authority, build, APK or external publication was changed in this step.

## V4.16 — Pump.fun fundamentals and verified DeepSeek evidence memory (in progress)

- 2026-08-05, Codex Work Mode. Serge explicitly scoped the two accumulated items as the next version and authorized publication: an internal Pump.fun ecosystem layer without a new screen, plus the previously specified verified 50-MiB DeepSeek memory and human-readable analysis. Started the single branch `agent/v4-16-ecosystem-evidence-memory` from merged V4.15 commit `3b4c3e82336d32fa165f22a4782e362ee3281689` and carried forward the documentation backlog as `7c12c4b`.
- Added `PumpEcosystem.kt`. A ten-minute, failure-isolated background client polls the public Solana migrator account, DefiLlama DEX/fee summaries and Pump.fun buyback statistics. It produces age/quality-labelled migrations and BOOST triggers, volume, revenue, buyback and available burn facts. Missing fields remain null; the ecosystem score is context only and cannot execute or veto a trade.
- Added `DeepSeekEvidenceMemory.kt` with a local SQLite database. Each non-neutral primary DeepSeek forecast is frozen with price, action, direction, confidence and compact feature key, then filled with first available outcomes after 15m/1h/3h/6h/24h. Pattern scoring enforces horizon spacing for independence, a 30-case minimum, >=60% directional precision overall and on the newest walk-forward slice, positive expectancy after 0.30% round-trip fees and lift over the local baseline. Evidence older than 180 days falls out of scoring, so stale patterns decay and are demoted automatically.
- The first allocation is capped at 50 MiB. At 95% the existing main activity presents `+50 МБ`, prune-low-value and decide-later choices together with evaluable/promotion/usefulness statistics. The model receives at most three promoted and two background summaries for the matching current regime, never the database itself.
- Added Pump.fun and evidence-memory JSON to the primary DeepSeek frame, its adversarial trade verifier and Serge-position supervisor. Expanded the Russian structured result with now/15–60m/3–24h/invalidation/uncertainty fields. A strong disagreement between fresh, sufficiently complete ecosystem and breathing scores selects Pro; existing BUY/EXIT verification, rapid-drop/freshness controls and portfolio separation remain unchanged.
- Advanced metadata once to V4.16/code 71 and updated README, workflow checks/artifact names, durable rules, provider diagnostics and self-diagnostics. Added focused pure tests for source-null honesty, ecosystem acceleration, independent-case spacing, the 60%/commission rule and walk-forward promotion. Static `git diff --check` and Android XML parsing pass. Local Gradle cannot download Gradle 8.5 because `services.gradle.org` is unreachable in this workspace; clean GitHub Actions Kotlin/Android compilation, full unit tests, lint and APK assembly remain required before delivery.
- Published the exact local V4.16 tree `1b4ad5050bdee1a717d0079d72a148a1d1cfa2ae` as remote commit `80ff4058297e4a728beead7b1170bf2c252d5985` and opened draft PR #39. GitHub Actions run #174 (`30986970446`) passed Kotlin/Android compilation, all unit tests, lint, `assembleDebug`, package/version/activity/ZIP verification and artifact upload. Artifact `PumpSignal-V4.16-Intermediate-APK` ID `8922560557` has archive SHA-256 `8e9a6f8e8215989726cba2429d8eb17bdaa9b8a3e17db8d8be175148e62076ea`; its single intermediate APK is 7,215,332 bytes with SHA-256 `edcc326f1fc56acfa99bb35d88851a1c0605f091f25fc565e74f5dfbbb23ae89`.
- Re-signed that exact verified APK with the installed-compatible update key. Final `PumpSignal-V4.16-Compatible-FINAL.apk` is 7,297,327 bytes with SHA-256 `5abbbbb0994893759b35544f63aa636d0ae84c088a9cd6718318e693a0e4a282`; APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.15. A clean repeat CI on this documentation head, independent payload comparison and merge to `main` remain required.

## V4.17 — actionable entry alerts and corrected ringing (in progress)

- 2026-08-05, Codex Work Mode. Serge rejected distant «prepare» calls and requested the first yellow entry warning only near 70–80%, further alerts on each ten-point improvement, clear participant/reason text, ordinary ringing only Monday/Tuesday/Thursday/Friday 06:15–23:00, guaranteed exit alarms for his open position and a selectable melody. Started `agent/v4-17-action-alerts` from merged V4.16 commit `6bd50b804dc09c71c6ebc5ec7cd4f96b5a0c87de`.
- DeepSeek entry alerting now begins at 7/10 and records each increasing integer through 10/10. Levels 7–8 are yellow and 9–10 green; lower preparation states remain visible in the app but do not notify. Alert text includes the current rationale and up to two model-supplied evidence items.
- Added a workday-aware sound schedule. Ordinary alerts and virtual trades ring only on Monday, Tuesday, Thursday and Friday from 06:15 inclusive to 23:00 exclusive; off-hours delivery uses a dedicated silent notification channel. A one-time migration resets legacy 24-hour/custom schedule state to the agreed work schedule.
- Only urgent protection of Serge's already-open position bypasses that schedule. Confirmed DeepSeek/Gemini exit danger, local personal guard and position rapid drop remain loud around the clock; cancellation/improvement notifications are always silent, while virtual entries/exits remain silent off-hours. Participant trade titles and reasons were made explicit.
- Added an Android ringtone picker and selectable V4.17 alarm channels. Advanced metadata once to V4.17/code 72 and updated README/workflow/durable rules. Focused tests cover workdays/boundaries, 7–10 alert steps and the urgent-personal-exit-only sound bypass. Full unit tests, lint, Android compilation, APK inspection, compatible signing and publication remain required.
- Published the exact 12-file implementation tree `080a7866d8af277bed9333f480a61eeda318f15d` as remote commit `237c474c6ed428f10d902c6c7b830b526233ea7a` and opened draft PR #40. GitHub Actions run #178 (`31013001116`) passed the complete Gradle route: Kotlin/Android compilation, all unit tests, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.17/code 72, launch activity, intermediate signature, ZIP integrity and artifact upload.
- Run #178 artifact `PumpSignal-V4.17-Intermediate-APK` ID `8933385479` has archive SHA-256 `b82694bd37401bedd09dcd2a5a03bd71aeb3d6d922e572c9da0557fcc7aac491`; the contained intermediate APK is 7,217,764 bytes with SHA-256 `ca1a5ff08ba730cc2d98b8deb109d61740e61c76e83707300985ff1886c5861e`.
- Re-signed that exact CI APK with the installed-compatible update key. Final `PumpSignal-V4.17-Compatible-FINAL.apk` is 7,301,423 bytes with SHA-256 `5721086fc0598f5923cf01f3b14ef6962891dbb9c315a5b7658aefa4e56240a3`; APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.16. `AndroidManifest.xml`, `resources.arsc` and all four DEX entries are byte-identical to the CI APK. A clean repeat CI on this documentation head and merge to `main` remain required.

## V4.18 — stabilized DeepSeek decisions and material-only alerts (in progress)

- 2026-08-07 14:07 CEST, Codex Work Mode. Serge reported repeated DeepSeek notifications every few minutes, a contradictory `9/10` exit-danger card while local sale readiness remained unconfirmed, an inactive main DeepSeek account, nervous experiment trades, excessive explanation text, poor handling of PUMP/BTC lead-lag and a suspected remaining +8% exit ceiling. Started the single branch `agent/v4-18-deepseek-stability` from canonical V4.17 `origin/main` commit `648eef7`.
- Root-cause review found that any isolated APP `sellSignal` or rapid-drop flag forced the user exit scale directly to 9/10; position alerts fired on every changed `conditionDelta` and could re-alert after 9→8→9 oscillation; the entry alert band reset immediately at 6/10; one-minute BTC weakness could hard-cap an otherwise independently strong PUMP entry; the experiment still had a special +8% pullback exit; and the StrategyV2 backtest still sold the full trend position at +8% although runtime had begun winner accompaniment.
- Added persisted position-alert watermarks. The first confirmed EXIT alerts immediately; later alerts require a new worst danger level or at least two additional condition-delta points, with a ten-minute ordinary repeat floor and a one-time immediate escalation to 10/10. Improvements and cancellation update the card without another notification. The free local guard no longer repeats an unchanged warning periodically; it must first observe a real recovery interval. Entry-alert series retain their peak through 6↔7 noise and may restart only after a real retreat to 4/10 plus a 15-minute new-series cooldown.
- Recalibrated exit danger: an isolated APP sell is warning level 6, rapid drop requires concurrent seller takeover to reach 8, DeepSeek-confirmed exit reaches 9 and the live local emergency guard reaches 10. BTC is now a regime filter: sustained independent PUMP 5/15-minute flow can survive a short BTC divergence, while joint multi-horizon weakness remains blocking. A model `WATCH/HOLD` with its own 9–10 readiness, direction/confidence and independent local confirmation is normalized to a proposed BUY and still passes the existing adversarial verification before execution.
- Removed the remaining +8% profit behavior from the experiment, live APP trend exit and historical backtest. Trend positions activate a no-fixed-target trailing runner after +4% and may continue beyond +20% until a later confirmed 4% pullback, emergency stop or time limit. The experiment evaluates at most once per two minutes and requires five strong evidence groups for an immediate reversal; moderate reversals still require repeated confirmation.
- Shortened the main DeepSeek card to the current conclusion, any rejected-trade reason, nearest scenario, concrete reconsideration condition and update time. Advanced metadata once to V4.18/code 73 and updated README/workflow/durable rules. Static `git diff --check` passes. Local Gradle could not download Gradle 8.5 because `services.gradle.org` is unreachable; full Android CI, unit tests, lint, APK inspection, compatible signing and publication remain required.
- Created the clean local implementation checkpoint `c948cc4` on `agent/v4-18-deepseek-stability`. It contains the 16 intended strategy, alert, test, metadata and coordination files; the worktree was clean immediately after the checkpoint. The mandatory publication workflow cannot proceed in this environment because the required GitHub CLI is not installed (`gh: command not found`); no remote branch, PR, CI result or V4.18 APK is claimed yet.
- 2026-08-07 14:24 CEST, Codex Work Mode. Serge supplied V4.17 screenshots showing a rising 15-minute chart and fresh PUMP aggressive-buy readings of 85%/63%/57.2% on 15s/60s/5m while the position supervisor still emitted `EXIT`, danger 10/10 and incorrectly described the 63% buyer share as strengthening sales. Root cause: the model's position EXIT had no deterministic post-response corroboration, and the action scale treated any local guard alarm delivered in the previous ten minutes as still-current critical evidence.
- Added `PositionExitConfirmationPolicy` in `PositionSupervisor.kt`. A DeepSeek EXIT now needs the model plus at least two corroborating fresh/local evidence groups, with actual PUMP selling, weak spot flow or a live local safety event mandatory. A hard −4.4% stop and an unrecovered rapid drop confirmed by current seller flow remain immediate. A rising price with >=60% aggressive buys on 15s/60s and >=54% on 5m rejects a contradictory new EXIT, caps danger at 5 and converts an already-advised exit to `CANCEL_EXIT`. The prompt now explicitly defines buy percentages above 50 as buyer dominance and forbids a stale peak pullback from independently producing 10/10 during such a rebound.
- Made local-guard criticality current-state-based and backward-compatible. `PersonalPositionGuardState.criticalActive` is recalculated each cycle and persisted with a safe false default for older JSON. An isolated APP sell remains warning-only; recovered rapid-drop context clears criticality. `DeepSeekActionLevel` no longer interprets a historical alarm timestamp as proof of present danger.
- Added focused regression coverage in `PositionSupervisorTest.kt` for the exact 85%/63%/57.2% rebound, stale-exit cancellation and a genuine multi-group seller takeover, plus `PersonalPositionGuardPolicyTest.kt` for recovered rapid-drop and isolated APP-sell behavior. Static `git diff --check`, package `com.example.pumppaperbot.v8`, V4.18/code 73 and balanced Kotlin delimiters pass. Gradle tests could not start because the wrapper cannot reach `services.gradle.org`; no compilation, CI, APK, signature or publication is claimed for this checkpoint.
- Saved this seven-file correction on `agent/v4-18-deepseek-stability` as local commit `e4ca503` (`Reject contradictory DeepSeek exit alarms`). It is based on the earlier V4.18 checkpoint `15e0280`; the worktree was clean after the commit. The branch remains local and uncompiled, with no remote PR or APK claimed.

## V4.19 — release of DeepSeek stabilization with recovery hysteresis (in progress)

- 2026-08-07 15:37 CEST, Codex Work Mode. Serge observed that the phone still showed V4.17 and deliberately asked to skip the never-built V4.18 APK and release the accumulated correction as V4.19. Confirmed that V4.17/code 72 is the last compiled compatible APK; V4.18/code 73 existed only in the local `agent/v4-18-deepseek-stability` source branch and was never installed or rolled back on the device.
- Carried every V4.18 strategy/alert correction into V4.19 and added persisted two-stage recovery hysteresis for an already-issued personal-position EXIT. A strong fresh rebound (rising 60-second price plus buyer dominance on 15s/60s/5m) cancels a contradictory stale EXIT immediately. A moderate rebound must remain buyer-supported for two consecutive supervisor cycles before cancellation; the first cycle lowers the unconfirmed danger to yellow and records the recovery without ringing. Renewed selling, the hard −4.4% stop and an unrecovered rapid drop confirmed by current seller flow reset/override recovery.
- Added backward-compatible `exitRecoveryStreak` persistence and focused tests for first-cycle hold, second-cycle cancellation and renewed-selling rejection. Advanced metadata once from the unreleased V4.18/code 73 checkpoint to V4.19/code 74; updated README, workflow artifact/checks and durable rules. Package remains `com.example.pumppaperbot.v8`. Full unit tests, lint, Android compilation, compatible signing, direct APK verification, publication and merge remain required.
- Static `git diff --check` and parsing of every Android resource XML file pass. The local full Gradle route was attempted with a workspace-owned cache but stopped before Kotlin compilation because `services.gradle.org` is unreachable and no complete Gradle 8.5 distribution is cached. This is an environment bootstrap failure, not a test result; clean CI remains mandatory.
- Published the exact 17-file V4.19 implementation tree `df80b47ca7cb6114b4dcb6c4c302afe030382651` as remote commit `eddab0b048658f0bbe1c1295ed4afc4e2f40b965` on `agent/v4-19-deepseek-stability` and opened draft PR #41. GitHub Actions run #181 (`31183898069`) passed Kotlin/Android compilation, all unit tests including the new recovery-hysteresis cases, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.19/code 74, launch activity, intermediate signature, ZIP integrity and artifact upload.
- Run #181 artifact `PumpSignal-V4.19-Intermediate-APK` ID `8996027254` has archive SHA-256 `38cac0f3f25e403842775cb6795882e5c48a086c1f0243c789437e93012d221a`; its contained intermediate APK is 7,225,972 bytes. Re-signed that exact APK with the installed-compatible personal update key. Final `PumpSignal-V4.19-Compatible-FINAL.apk` is 7,309,615 bytes with SHA-256 `3123d1827e5e3ebc8a843b6b6c31b7c7ed614c7d1b28af3098e0e88b5d59eed2`; APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching installed V4.17. `AndroidManifest.xml`, `resources.arsc` and all four DEX entries are byte-identical to the CI APK. A clean repeat CI on this documentation head and merge to `main` remain required.
- Documentation head `dd4ee035088b18b84a13ee0bab8c8f907d24f885` passed a second complete GitHub Actions run #182 (`31184407776`). Repeat artifact ID `8996214590` has archive SHA-256 `721894f8c8ceccf2262d279f9c60b642ef32bdce0baca18367234a945e0daf28`; its independently rebuilt `AndroidManifest.xml`, `resources.arsc` and all four DEX entries exactly match run #181 and the signed final APK. Marked PR #41 ready only after this result and merged it into canonical `main` as `9cd814c78d74ae7a78f59072761030db3acde164`. Source integration is complete. Immutable `v4.19` tag, GitHub Release and durable Release-asset upload remain unavailable in the connected GitHub toolset and are not claimed.

## V4.20 — APP-led hybrid with calmer DeepSeek (in progress)

- 2026-08-08 20:35 CEST, Codex Work Mode. Serge reported that V4.19 DeepSeek repeatedly recommended exits near local lows, stayed at entry level 1/10 during a roughly four-percent rebound, missed subsequent recovery candles, and that the experiment churned on micro-fluctuations while APP remained comparatively stable and profitable. Started the single clean branch `agent/v4-20-app-led-hybrid` from the completed local V4.19 tree.
- Root-cause inspection found that the V4.19 entry scale gave only 10% direct weight to APP readiness while model readiness/direction/confidence contributed about 65%; a pessimistic model response could therefore erase an APP setup. The main DeepSeek paper account could also execute its own verified EXIT without APP phase confirmation, and the experiment could enter on one eligible evaluation and close after two two-minute danger checks. Virtual BUY notifications were intentionally suppressed while Serge's position was open, and Android could retain muted state on the V4.17 channel ids.
- Added `AppLedHybridPolicy.kt`. APP readiness and actual APP phase are now the stable baseline; 5/15-minute buyer flow confirms a rebound, DeepSeek adjusts the baseline by a bounded amount on a 30–90-minute role, and short Bitcoin divergence is a regime clue rather than a direct block. A normal exit requires APP confirmation or simultaneous 15/30/60-minute weakness plus current selling. Fresh 15s/60s/5m buyer recovery rejects a contradictory exit; the −4.4% personal hard stop and confirmed emergency fall remain immediate.
- Rewired the visible action level and primary DeepSeek paper action through the APP-led fusion. An APP setup near 70–80 with confirmed buyer recovery can produce a yellow 7–8/10 even when the model remains bearish; a BUY still requires the APP/DeepSeek/flow combination and the existing independent trade-verification call. Prompts now explicitly treat APP as pilot, distinguish an unproven stop-hunt hypothesis from fact and forbid micro-noise from independently causing EXIT.
- Smoothed the DeepSeek experiment: independent entry needs two spaced confirmations; ordinary exits require a minimum 20-minute hold and three confirmation cycles, while APP-backed or very strong structural reversals still need two cycles; re-entry is paused for 20 minutes after an exit. The −5% experimental emergency remains immediate and no +8% cap was restored.
- Restored notifications for every actual virtual BUY and SELL even while Serge has an open position. All selectable alert/trade channels use new V4.20 ids so Android creates fresh high-importance sound channels; ordinary sound still obeys Monday/Tuesday/Thursday/Friday 06:15–23:00, while urgent confirmed Serge exits remain always loud.
- Found and closed a separate post-sale race: a DeepSeek or Gemini position request started before `Я продал` could finish after the sale and rewrite an old EXIT/notification over the cleared state. Both success and failure paths now re-check the current position id before saving, stale responses are discarded, and the DeepSeek/Gemini/local-guard notifications are cancelled immediately on sale, reset or detection of a closed/replaced position.
- Advanced metadata once to V4.20/code 75 and updated README, workflow checks/artifact names and durable rules. Added focused policy tests for the observed APP≈74/DeepSeek=1 rebound, APP-led BUY, BTC sideways/PUMP strength, micro-only EXIT rejection, persistent APP-backed exit, recovery cancellation, two-cycle experiment entry and three-cycle ordinary exit. `git diff --check` passes. Local Gradle stopped before compilation because `services.gradle.org` is unreachable and no complete Gradle 8.5 distribution is cached; Kotlin compilation, full unit tests, lint, APK assembly, compatible signing and publication remain required.
- Recorded the complete 18-file implementation as local commit `a9db6dda44b1722505e6212fa99667ef4738f2a6` on `agent/v4-20-app-led-hybrid`. XML parsing, workflow YAML parsing, `bash -n gradlew` and cached-diff checks pass. This environment has neither a complete Gradle distribution nor the mandatory `gh` CLI, so no compiled APK, CI run, signature verification, remote branch or pull request is claimed yet.

## V4.21 — independent but persistent DeepSeek authority (in progress)

- 2026-08-08 22:55 CEST, Codex Work Mode. Serge asked for a final re-check that normal DeepSeek retained its own strength and that DeepSeek experiment was genuinely calmer. V4.20 was not built or released. Root-cause review found that its supposedly independent DeepSeek BUY still required APP readiness at least 55/100, so the AI paper account could remain inactive despite a strong model/5–15-minute setup. The experiment's two-cycle entry compared exact fresh timestamps, which could change each cycle and repeatedly reset confirmation to 1/2. The active unpublished branch is renamed from `agent/v4-20-app-led-hybrid` to the single continuation `agent/v4-21-dual-authority`; no parallel release branch is created.
- Promoted the work to V4.21/code 76. APP keeps its trusted immediate entry lane, while normal DeepSeek receives a separate autonomous lane: model BUY, readiness at least 8/10, direction at least 60, confidence at least 65, sustained 5/15-minute buyer confirmation, no structural weakness/systemic BTC risk and no APP sell. Independent DeepSeek BUY and DeepSeek-only normal EXIT must persist across two separate AI cycles at least one minute apart; APP-confirmed actions and real emergencies retain their shorter paths. Persisted confirmation counters and raw `modelIntent` are backward-compatible additions to the existing DeepSeek state.
- Fixed experiment persistence by grouping changing DeepSeek/breathing timestamps into a stable 15-minute setup anchor. Independent experiment entry now needs three spaced confirmations; ordinary holding and re-entry cooldown are both 30 minutes; ordinary exit still needs three danger cycles and the −5% backstop remains immediate. Tiny simultaneous BTC/SOL declines no longer veto entry: the broad-market veto now requires material one-hour weakness without sustained independent PUMP continuation.
- Updated the primary DeepSeek prompt, action-level fusion, focused policy/experiment tests, selectable notification channel ids, README, workflow and metadata for V4.21. Package remains `com.example.pumppaperbot.v8`; storage keys and portfolio separation remain compatible with V4.19. `git diff --check` passes. Kotlin/Android compilation, full unit tests, lint, APK assembly, compatible signing, publication and merge remain required.
- Saved the complete 13-file V4.21 source checkpoint as local commit `2416631` on `agent/v4-21-dual-authority`. Android XML parsing, workflow YAML parsing, `bash -n gradlew` and `git diff --check` pass. A local Gradle attempt stopped before compilation because the wrapper cannot reach `services.gradle.org`. GitHub publication/CI is also blocked in this workspace by the GitHub publish workflow prerequisite: `gh` is not installed, so no remote branch, PR, CI run or APK is claimed.
- 2026-08-08 23:13 CEST, Serge explicitly authorized building V4.21 and clarified that publication must use the connected direct GitHub access rather than depend on the absent `gh` executable. Rechecked the local clean branch, package `com.example.pumppaperbot.v8`, V4.21/code 76, signing configuration and all 18 changed paths. Remote `main` commit `3c0a03327fa15bf1496589279f70d4899df7fe7e` has the same source tree as the local V4.19 base, so V4.21 can be published as one clean commit on `agent/v4-21-dual-authority` without carrying parallel history. CI, artifact verification, compatible signing, merge and release completion remain the next steps.
- Published the exact 18-file V4.21 tree `7dcdeb0b692e039d87ca6993bcf8d92cd4c94dde` through the connected GitHub app as remote commit `60e20877051b350209a928566c222e52b5f8aab2` and opened draft PR #42. GitHub Actions run #185 (`31278886781`) passed Kotlin/Android compilation, all unit tests, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.21/code 76, launch activity, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V4.21-Intermediate-APK` ID `9027856758` is 6,267,173 bytes with archive SHA-256 `c2987a7a1b027b3441b56a98162a2751273eb7cc6f22c6a394af01ce5bc7ff99`; its contained intermediate APK is 7,242,696 bytes.
- Re-signed that exact CI APK with the installed-compatible update key. Final `PumpSignal-V4.21-Compatible-FINAL.apk` is 7,325,999 bytes with SHA-256 `618deb415e6b0dbfd049c905420df94dbfba4d0178f2ac1522b5ee74ab48c6b9`; APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.19. `AndroidManifest.xml`, `resources.arsc` and all four DEX entries are byte-identical to the CI APK. Direct inspection also confirms version 4.21, package `com.example.pumppaperbot.v8`, `MainActivity`, Android 13+ notification permission and the separate V4.21 APP/DeepSeek/experiment/position channels. A clean repeat CI on this documentation head and merge to `main` remain required.
- Documentation head `290fc87c72f6c1fa58ddfcfa33410cf6fcf0796a` passed a second complete GitHub Actions run #186 (`31279165307`). Repeat artifact ID `9027937603` has archive SHA-256 `8deeeac40e0c2989f1a13f5e7a15f2e7194b95be8c49914f2ba4eb112a48e50f`; its independently rebuilt `AndroidManifest.xml`, `resources.arsc` and all four DEX entries exactly match run #185 and the signed final APK. Marked PR #42 ready only after this result and merged it into canonical `main` as `3c6a4656e4df599c202f976ab0e41f9e3fa66103` at 2026-08-08 23:27 CEST. Source integration and compatible APK completion are done. Immutable `v4.21` tag, GitHub Release and durable Release-asset upload remain unavailable in the connected GitHub toolset and are not claimed.

## V4.22 — reliable ringing and channel tests (in progress)

- 2026-08-09 12:38 CEST, Codex Work Mode. Serge confirmed the proposed sound correction after V4.21 delivered visible messages without ordinary ringing on Sunday. Started the single bounded branch `agent/v4-22-reliable-ringing` after confirming that GitHub had no existing V4.22 branch or open PR. Root-cause review found three independent problems: V4.21 routed executed virtual trades through the workday-only preparatory schedule, `AlertSchedule.isAllowedNow()` ignored the stored 24-hour mode, and opening `AlertSettingsActivity` forcibly reset the mode to workdays.
- Separated notification delivery by event class. Preparatory APP/DeepSeek signals retain the saved workday/daily/24-hour mode. Executed APP, DeepSeek and DeepSeek-experiment BUY/SELL trades ring every day from 06:15 to 23:00 in workday or daily mode and ring around the clock in 24-hour mode. Confirmed urgent Serge exits still bypass every quiet window. All notifications remain visible through the silent channel when their sound window is closed.
- Added persistent workday, daily and 24-hour controls without screen-open reset; fixed the pure schedule evaluator so daily and always modes are actually honored. Added separate APP, DeepSeek, experiment and Serge sound-test buttons plus a direct Android notification-settings link. The settings page is now scrollable so the expanded controls remain reachable.
- Advanced metadata once to V4.22/code 77 and moved all selectable alert/trade/position/silent channel ids to fresh V4.22 names, preventing Android from inheriting a muted V4.21 channel. Updated README, workflow checks/artifact names, durable rules and focused unit cases for workday boundaries, daily/24-hour modes, daily executed-trade delivery and urgent personal exits. Files changed on `agent/v4-22-reliable-ringing`: `AlertSchedule.kt`, `AlertSettingsActivity.kt`, `DeepSeekActionLevel.kt`, `PumpAlert.kt`, two unit-test files, build/workflow/README/AGENTS and this log. Static verification and full Android compilation remain pending; no commit, remote branch, PR, APK or release is claimed yet.
- Static `git diff --check`, Android resource XML parsing, workflow YAML parsing and Gradle-wrapper shell syntax pass. No active channel id, workflow check or build metadata remains on V4.21. A local full Gradle attempt used a workspace-owned cache but stopped before Kotlin compilation because `services.gradle.org` is unreachable and no Gradle 8.5 distribution is cached. This is an environment bootstrap failure, not a test result; clean GitHub Actions compilation, unit tests, lint and APK assembly remain mandatory.
- Saved the complete 11-file V4.22 implementation and coordination update as local commit `8a6b8c4` on `agent/v4-22-reliable-ringing`. The worktree was clean immediately after the commit. GitHub publication will use the connected direct app, not `gh`, under Serge's standing PumpSignal authorization; remote tree comparison, PR, CI and compatible APK verification remain pending.
- Published the exact local V4.22 tree `cb3d1639cca801cefe64d382615307d3d73c602a` through the connected GitHub app as remote commit `8ccf3724a5c4f9563b26db09a7d02098456dd343` on `agent/v4-22-reliable-ringing` and opened draft PR #43. The branch is one commit ahead of remote `main`, has no divergence and contains exactly the 11 intended paths.
- GitHub Actions run #189 (`31308956511`) completed successfully: Kotlin/Android compilation, all unit tests including the new schedule/delivery cases, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V4.22/code 77, launch activity, intermediate v2 signature, ZIP integrity and artifact upload all passed. Artifact `PumpSignal-V4.22-Intermediate-APK` ID `9036836535` is 6,274,198 bytes with archive SHA-256 `c47694fa95630f192a6a3b88b6961230af9042162787d1b717961d949db38b30`; its intermediate APK is 7,247,848 bytes.
- Re-signed that exact CI APK with the installed-compatible update key. Final `PumpSignal-V4.22-Compatible-FINAL.apk` is 7,330,095 bytes with SHA-256 `1c7b808e2a11c378392b54807998b45286bb6933052a92b918b364b2aa52b267`. APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.21. `AndroidManifest.xml`, `resources.arsc` and all four DEX entries are byte-identical to the CI APK. Direct DEX inspection confirms all eight V4.22 selectable/silent channel ids and no V4.21 channel id. A clean repeat CI on this documentation head and merge to `main` remain required.
- Documentation head `37d28cb3d002e6766951b9f4fa46388cffb04ee2` passed a second complete GitHub Actions run #190 (`31309256198`). Repeat artifact `9036917784` has archive SHA-256 `59ee175f5cec8565f2d87fb8539394ea125b767b5bde3a64df641d26bd05ca56`; its independently rebuilt `AndroidManifest.xml`, `resources.arsc` and four DEX entries again match the signed final APK. Marked PR #43 ready only after this result and merged it into canonical `main` as `ba9d91c2e72c95d73742a93cbd009c394c50c3d2`. Source integration and compatible APK completion are done. The connected GitHub toolset still exposes no immutable tag/Release-asset upload or branch-delete operation, so `v4.22`, a GitHub Release asset and deletion of the finished temporary branch are not claimed.

## V5 — causal research engine and evidence gate (research only)

- 2026-08-16, Codex Work Mode. Serge rejected V4.22 signal quality after observing late entries near local tops and exits on ordinary volatility, and requested a deep GitHub/internet review rather than another quick threshold patch. Audited the latest V4.22 tree and screened 50 open-source execution, backtesting, crypto and LLM/ML trading projects. The central defect is architectural: `StrategyV2.backtest` does not replay the actual APP + DeepSeek + experiment decision path shown to the user, while several live policies combine manually chosen fixed thresholds and model self-reported confidence. The optional historical research test is skipped without an external fixture, so normal CI does not establish profitability. A suspected one-month limit on the one-year backtest was investigated and rejected: the result activity separately paginates the requested range; the shorter limit applies to the live cache only.
- Started `agent/v5-research-engine` from the completed local V4.22 tree. Added `docs/V5_RESEARCH_ARCHITECTURE_RU.md` with the 50-project research trail, audit findings, proposed DeepSeek risk-review contract and frozen validation protocol. Added a pure `ResearchDecisionEngine` foundation with explicit market regimes, three non-chasing setups, mandatory `NO_TRADE`, ATR/percentile normalization, missing-flow rejection and an independent validation gate. Without qualifying walk-forward and shadow evidence, a detected setup is only `SHADOW_CANDIDATE` and cannot become an actionable signal. Added `ResearchPositionEngine`: an ordinary two-percent pullback cannot independently force an exit; closed invalidation, a trail activated only after +2 ATR, confirmed structural failure or the research horizon is required. Added a causal `ResearchReplayEngine` which uses those same entry/position functions, fills only at the next candle open inside the precomputed entry zone, refuses to chase a gap and reports fee/slippage-adjusted trades, expectancy, profit factor and mark-to-market drawdown. Focused tests cover the evidence lock, anti-chase behavior, missing-data refusal, ordinary-pullback hold, invalidation, next-open replay, gap refusal and immunity of a past decision to future candles. Nothing is wired to alerts, no version is advanced and no APK/publication is claimed; external historical validation and forward shadow verification remain mandatory.
- 2026-08-17 18:14 CEST, Codex Work Mode. Serge kept the V5 autonomous paper requirement but requested one explicit master button to enable or disable calls while preserving the V4.22 schedule, and asked to retain manual `Я купил` / `Я продал` support with the app acting as an evidence-first analyst. Replaced the hardcoded silent gate with persisted `ResearchModePolicy` alert state, default OFF. Turning it off immediately cancels user signal/trade/position/test notifications, vibration and pending entry reminders; turning it on restores the saved workday/daily/24-hour delivery policy. Schedule selection remains editable while calls are off. APP, DeepSig and DeepSigX analysis, AI cycles, journals and virtual trades remain independent of the switch, and the foreground monitor notification remains silent.
- Promoted the branch once from V4.22/code 77 to V5.0/code 78 while preserving package `com.example.pumppaperbot.v8`. Added fresh V5 notification channel ids so Android does not inherit a muted V4.22 channel, made the main screen show the master state, and reframed APP output as a candidate plus reason for manual verification rather than an automatic real-money instruction. Updated `ResearchModePolicy.kt`, `AlertSettingsActivity.kt`, `PumpAlert.kt`, `EntryAlertReminder.kt`, `MainActivity.kt`, `PumpSignalService.kt`, the paper-account screens, layout, policy test, `app/build.gradle`, workflow, README, architecture document and durable repository rules. Compatible final target is `PumpSignal-V5.0-Compatible-FINAL.apk`; compilation, full tests, lint, APK signing/inspection, publication and delivery are not yet claimed.
- Static `git diff --check`, Android XML parsing, workflow YAML parsing and Gradle-wrapper shell syntax pass; active build/workflow metadata is V5.0/code 78 and active selectable notification channels use V5 ids. A local full `testDebugUnitTest lintDebug assembleDebug` attempt stopped before Kotlin compilation because Gradle 8.5 is not cached and the wrapper cannot reach `services.gradle.org`. The workspace contains the prior compatible APK and update keystore but no configured signing passwords, and the mandatory `gh` publish prerequisite is absent. Therefore no compiled V5 APK, signature result, remote commit, PR, CI run or download is claimed at this checkpoint.
- Saved the 18-file V5.0 master-switch and release-metadata change as local commit `19af697` (`Add V5 master alert switch`) on `agent/v5-research-engine`. The earlier causal research implementation remains in parent commit `d1189c9`; together they form the current V5.0 source checkpoint. No remote publication or APK is implied by the local commits.
- 2026-08-17, direct GitHub publication authorized by Serge. Published the exact 31-file local V5.0 tree on remote `agent/v5-research-engine` as commit `39ecbcc02c4afe1a43cb985f316d029c020ea8c7` and opened draft PR #44 against `main`. GitHub Actions run #193 (`32049325763`) compiled the Android/Kotlin source successfully, then stopped in unit tests: two assertions still expected obsolete Russian wording although both protected behaviours passed. Updated only those assertions to match the V5 messages (`кадр уже использовался`, `вне зоны`); no decision, execution or risk rule changed. A clean CI rerun and compatible APK verification remain required.
- Published the focused assertion correction as remote commit `c628819f58a2de6b47881f6dd850567ba5f57c2e`. GitHub Actions run #194 (`32049690677`) passed Android/Kotlin compilation, all unit tests, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V5.0/code 78, launch activity, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V5.0-Intermediate-APK` ID `9294303425` is 6,299,116 bytes with archive SHA-256 `6366ff99f53bf1daf01b21b50f7c5276bc596e8b514e4aca7daa000e23cdbfdf`; its contained intermediate APK is 7,275,276 bytes with SHA-256 `d355099d4d89db6ade26807b9f660ae979b316bf9f96ab9653eccce1c6cb92c8`.
- Re-signed that exact CI APK with the installed-compatible personal update key. Final `PumpSignal-V5.0-Compatible-FINAL.apk` is 7,358,767 bytes with SHA-256 `8e2909a53a13741e1177438223167f3ed337f0fb7d5c0f50e9eb400c570d09fc`; APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V4.22. All 842 non-signature ZIP payload entries, including `AndroidManifest.xml`, `resources.arsc` and four DEX files, are byte-identical to the CI APK. A clean repeat CI on this documentation head, merge to `main` and durable GitHub Release upload remain required.
- 2026-08-17, Codex Work Mode, V5.1 implementation checkpoint on `agent/v5-1-fusion-readonly`. Added Android-Keystore-protected Bitpanda Fusion credentials, a GET-only `PUMP-EUR` orderbook client, fail-closed freshness/spread/depth evidence in the DeepSig frame, and a fourth isolated €1 000 `DEEPSIG FUSION` paper account which accepts only post-activation DeepSig decisions and executes virtually at Fusion ask/bid with fees. Added the Bitpanda center, main/competition controls, a 30-day sanitized four-agent journal plus shareable unified JSON via FileProvider, parser/execution/idempotency tests, V5.1/code 79 metadata, workflow checks, README and durable V5.1 safety rule. Relevant files include `.github/workflows/android.yml`, `AGENTS.md`, `README.md`, `app/build.gradle`, manifest/layout, the worker/service/DeepSig/main/competition integrations, and new `BitpandaFusion*.kt`, `FusionSim.kt`, `UnifiedResearchLog.kt`, `share_paths.xml`, `BitpandaFusionTest.kt`. `git diff --check` passes and a source scan finds no Fusion order/transfer/cancel call; local Gradle stopped before compilation because Gradle 8.5 is not cached and this sandbox cannot reach `services.gradle.org`. Full GitHub Actions compilation/tests/lint/APK inspection are required before merge, signing or delivery. No API key or secret was used or recorded.
- Published the exact V5.1 source tree as remote commit `5f1fcd939e38b08e0cf4ac359b9b2a5e377916a8` and opened draft PR #45. GitHub Actions run #197 (`32057374227`) passed Kotlin/Android compilation, all unit tests including Bitpanda orderbook/FusionSim coverage, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V5.1/code 79, launch activity, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V5.1-Intermediate-APK` ID `9296938063` is 6,331,279 bytes with archive SHA-256 `198fe2ea26f8ee0d16463982ec633bd63d407da37121568193bb249bf0452e9d`; its APK is 7,307,701 bytes with SHA-256 `68884eb87035d9193d77368bec12a76b4b3a8f5a111dc1c19b22367b63c70e9a`. Re-signed that exact CI APK with the compatible update key. Final `PumpSignal-V5.1-Compatible-FINAL.apk` is 7,391,604 bytes with SHA-256 `34dfce42e55c241eecea0bcfb163a3601aeb430aca155fe2cbe2ba0468ac46c5`; v2/v3 signatures verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`. All 843 non-signature payload entries are byte-identical to the CI APK. A clean repeat CI on this documentation head and merge remain required; GitHub Release asset publication is not yet claimed.
- Documentation head `86b2ab1f49ff1e66b67c19f1bb98851684a6621d` passed the complete repeat GitHub Actions run #198 (`32057851433`). Marked PR #45 ready only after that result and merged it into canonical `main` as `c92a201a2c52c7b942f0ba7d18601dde57e2755b`. Source integration and compatible local APK verification are complete. The connected GitHub toolset exposes no immutable-tag or Release-asset mutation, so a `v5.1` tag/GitHub Release download is not claimed; the verified local APK remains the delivery artifact.
- 2026-08-17, Codex Work Mode, V5.1.1 comparison-layout fix on `agent/v5-1-1-competition-layout`. Serge confirmed that all five charts existed but vertical scrolling slipped under the finger. Root cause: five fixed 220-dp `CompetitionChartView` children consumed horizontal gestures inside a vertical `ScrollView`. Removed that nested scroll container and gave all five charts equal weight in the remaining screen height, preserving synchronized horizontal offsets. Promoted once to V5.1.1/code 80 and made visible/log version labels derive from `BuildConfig.VERSION_NAME`; updated `CompetitionActivity.kt`, `MainActivity.kt`, `BitpandaFusionActivity.kt`, `UnifiedResearchLog.kt`, build/workflow metadata, README and durable rules. No trading, Bitpanda, key-storage, account, alert or strategy behavior changed. Local `git diff --check` and GitHub CI/build/signing remain required before delivery.
- Published the exact V5.1.1 tree as remote commit `2a1addb23947107b9f9352c851af1758c95b8e25` and opened draft PR #46. GitHub Actions run #201 (`32059628418`) passed Kotlin/Android compilation, all unit tests, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V5.1.1/code 80, launch activity, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V5.1.1-Intermediate-APK` ID `9297758424` is 6,331,483 bytes with archive SHA-256 `2aa29c5c2a6b2edaeb09679f7556b7d7f8537b46097856c4ba4978c2a32d120d`; its APK is 7,307,697 bytes with SHA-256 `b1d85fb8cd1475abd7c4e14ecdcaa8b153aec0c59f70eb9ba9a992b17a32c4bb`. Re-signed that exact CI APK with the compatible update key. Final `PumpSignal-V5.1.1-Compatible-FINAL.apk` is 7,391,604 bytes with SHA-256 `f4de159a43b41696132489e6eda01ec1c657acceaa65b1e4ceccb193048db4d7`; v2/v3 signatures and ZIP integrity verify, certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, and all 843 non-signature payload entries are byte-identical to the CI APK. Repeat CI on the documentation head and merge remain required before delivery.
- Documentation head `c2a8dea3563b6b27260c942b85db8dfd9191d87f` passed complete repeat GitHub Actions run #202 (`32060128262`). Marked PR #46 ready only after that result and merged it into canonical `main` as `97f3bf96382cabea4628691945efc0d133a4f4a7`. The V5.1.1 source, UI fix and compatible APK are complete; no strategy or stored-data migration occurred.
- 2026-08-17 21:59 CEST, Codex Work Mode, V5.2 Fusion-priority implementation on `agent/v5-2-fusion-priority`. Serge confirmed that a virtual Fusion BUY must receive the same analytical urgency as a personally important entry, while remaining separate from his manual account. Added a pure `FusionPriorityPolicy`: an open FusionSim position forces the primary DeepSig and its trade verifier to Pro and shortens the foreground cycle to one minute. The DeepSig frame now includes Fusion entry ask/time, fresh bid/ask, spread, fee-adjusted liquidation value/PnL, peak and pullback; a two-percent decline remains insufficient by itself. The managed virtual-position gate continues risk supervision when Fusion is open even if the main DeepSig paper account has already closed, while stale Fusion data still blocks virtual execution and no real order path exists. Added visible MAX CONTROL state to the main/Bitpanda screens, sanitized per-cycle priority metrics, start/stop journal events, policy/PnL tests, V5.2/code 81 metadata, README and durable rules. Serge's `Я купил`/`Я продал`, personal PnL, stores and supervision were not connected to Fusion. Static checks, Android compilation, full tests/lint, GitHub publication, compatible signing and APK delivery remain pending.
- Local `git diff --check`, Android resource XML parsing, workflow YAML parsing and `bash -n gradlew` pass for the V5.2 checkpoint. The full local Gradle route was attempted with an isolated workspace cache but stopped before Kotlin compilation because Gradle 8.5 is not cached and `services.gradle.org` is unreachable in this managed environment. This is an environment bootstrap failure, not a test result; clean GitHub Actions compilation, tests, lint and APK assembly remain mandatory.
- Published the exact 13-file local V5.2 tree `298214287442d39521fd58c3c23559023b2d250e` through the connected GitHub app as remote commit `fe631262f6e1c5a7148ec1ba4cc10fc5885c08ee` on `agent/v5-2-fusion-priority` and opened draft PR #47. GitHub Actions run #205 (`32063678143`) passed Kotlin/Android compilation, all unit tests including the new Fusion priority/PnL/cadence cases, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V5.2/code 81, launch activity, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V5.2-Intermediate-APK` ID `9299167968` is 6,338,041 bytes with archive SHA-256 `3510eab129c74647f61884c9248e1b8c53c93c386a5c77cd94852509f49b306c`; its contained intermediate APK is 7,312,533 bytes with SHA-256 `a160caef21840786c6340681c830c55df8ed2962a504ab6477a5ed7adabb8edb`.
- Re-signed that exact CI APK with the installed-compatible update key. Final `PumpSignal-V5.2-Compatible-FINAL.apk` is 7,395,700 bytes with SHA-256 `7ca3e6f04df6eb3630f84a9909075c2fcedf8a9387a2716fd13814d10ab99d13`; APK Signature Schemes v2/v3 and ZIP integrity verify, and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V5.1.1. All 843 non-signature payload entries are byte-identical to the CI APK; direct manifest-string inspection confirms package `com.example.pumppaperbot.v8`, version 5.2 and `MainActivity` with MAIN/LAUNCHER. A clean repeat CI on this documentation head and merge to `main` remain required before delivery.
- Documentation head `2ddc915cd1bdb81ae50dee2f7add5364a7a6de1d` passed complete repeat GitHub Actions run #206 (`32064249481`). Repeat artifact `PumpSignal-V5.2-Intermediate-APK` ID `9299390628` has archive SHA-256 `cd7c822a054a0915f617b9a524c97c859b82c228e0dde3b67b6bfe042e743bc4`; its independently rebuilt APK SHA-256 is `48d5ebb36d29a5916cdf8b1884171ace66fc362ed67f9b9f9ce07bb7c664d28d`, and all 843 non-signature entries again match the signed final APK. Marked PR #47 ready only after this result and merged it into canonical `main` as `2583029333c3aad41b7ef800a456e0e58e3536e0`. V5.2 source integration and compatible APK verification are complete. The connected GitHub toolset still exposes no immutable-tag or Release-asset write operation, so `v5.2` and a durable GitHub Release APK are not claimed; the verified local direct APK is the delivery artifact.
- 2026-08-18, Codex Work Mode, V5.3 personal-adviser implementation on `agent/v5-3-personal-adviser`. Reworked the post-`Я купил` card into an action-first Russian explanation with deterministic HOLD/WATCH/confirmed EXIT wording, urgency, current PnL/peak/pullback, trend, risk, a 30–90 minute scenario, explicit reconsideration condition, orderbook, executed flow and a separate BTC/PUMP regime. Extended the DeepSeek position contract with trend/risk/scenario fields and 1/3/6-hour BTC context while preserving V4.19 multi-group exit confirmation, hard stop and emergency precedence; an ordinary 0.5–2% decline, one red candle or one book wall remains insufficient for EXIT. Added a pure probabilistic BTC/PUMP regime classifier and tests. Its prior is based on 4,088 aligned Binance hourly observations from 2026-03-01 through 2026-08-18: same-hour correlation was +0.611, PUMP usually joined a fast BTC impulse, but PUMP more often pulled back in the following 1–6 hours and a guaranteed catch-up during BTC sideways was not supported. Added the Russian methodology note, V5.3/code 82 metadata, workflow checks, README and durable rule. Static diff/XML/YAML/wrapper checks pass; local Gradle cannot bootstrap because Gradle 8.5 is absent, so GitHub Actions compilation/tests/lint/APK inspection are mandatory before delivery.
- Published the exact 13-file V5.3 tree through the connected GitHub app as remote commit `202f8ae930a5bee77b204718f401634231c0cde5` on `agent/v5-3-personal-adviser` and opened draft PR #48. GitHub Actions run #209 (`32112561446`) passed Kotlin/Android compilation, every unit test including the new BTC/PUMP and friendly command cases, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V5.3/code 82, launch activity, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V5.3-Intermediate-APK` ID `9315517208` is 6,348,151 bytes with archive SHA-256 `3526587ef1a8e50a392e99cd9d9e1d36f62b285fce8fd95bf9947e2deb90c549`; its contained intermediate APK is 7,324,717 bytes with SHA-256 `fcd15ee0af691961af06adc8d7115c93828fe1d80ef9a1e31eca2618fe2c089c`.
- Re-signed that exact CI APK with the installed-compatible update key. Final `PumpSignal-V5.3-Compatible-FINAL.apk` is 7,407,988 bytes with SHA-256 `efe4a68257639544164ae9833e0f857a45c6c974e98467131921b6975ffa4c22`; APK Signature Schemes v2/v3 and ZIP integrity verify, and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V5.2. All 843 non-signature payload entries are byte-identical to the CI APK. Saved the final APK to ChatGPT Library path `/Биткоин/PumpSignal-V5.3-Compatible-FINAL.apk`. A clean repeat CI on this documentation head and merge to `main` remain required before final delivery.
- Documentation head `fc69acd5e8916d13c0fbd4a93f00d8a7908568c1` passed complete repeat GitHub Actions run #210 (`32113107314`). Repeat artifact `PumpSignal-V5.3-Intermediate-APK` ID `9315734232` has archive SHA-256 `0101095ab2ec0db0dd1043612c85279a9954d363f69d760fa9ba2ca325c63e26`; its independently rebuilt APK SHA-256 is `1c5eb599aecc0ca0b7c6099d3cc874b953624e0980edcc50accee6181e75297e`, and all 843 non-signature entries again match the signed final APK. Marked PR #48 ready only after this result and merged it into canonical `main` as `37bdf19d4b61b52a8b405bb3955bdc153379239e`. V5.3 source integration, compatible APK verification and Library delivery are complete.
- 2026-08-18 14:05 CEST, Codex Work Mode, V5.4 buyer-breath implementation checkpoint on `agent/v5-4-buyer-breath`. Serge requested a permanent graphical module, visible before and after `Я купил`, that recognizes the beginning, acceleration, maturity and ending of aggressive-buy inflows instead of treating a value such as 68% as automatic growth. Analyzed 26,496 closed Binance PUMPUSDT/BTCUSDT five-minute bars from 2026-05-18 through 2026-08-18 with the reproducible `research/buyer_breath_cycle_analysis.py`. Across 1,087 non-overlapping early inflow events, the next local price peak occurred between 5 and 50 minutes at the interquartile range and the additional maximum was +0.1122% to +1.1601%, but the 60-minute final return was positive only 47.65% of the time. This rejects a blind one-hour hold rule and supports a phase/price-response model. Added causal quiet/ignition/expansion/mature/exhaustion/seller-takeover/shock classification, buy-notional/trade-activity persistence, absorption risk, price-response efficiency, Russian action/watch explanations, a main-screen summary, a dedicated graphical activity with buyers-positive left and sellers-negative right, personal-position integration, and structured DeepSig context. Shock thresholds use the historical upper 0.5% absolute five-minute moves (PUMP 1.5475%, BTC 0.5600%) but preserve the existing emergency path. Advanced metadata once to V5.4/code 83 and updated workflow, README, research output/methodology, durable rules and focused phase tests. Static `git diff --check`, Android XML parsing and Python syntax pass; Android/Kotlin compilation, unit tests, lint, CI publication, compatible signing and APK delivery remain pending.
- Saved the exact 19-file V5.4 checkpoint as local commit `797fdd8` rebased directly onto canonical GitHub V5.3 head `7bdc8d77a9c2ebf39f458d4797a7d428355b682b`; the worktree was clean. A local Gradle attempt with a workspace-owned cache stopped before compilation because `services.gradle.org` is unreachable. The managed environment rejected the subsequent `git push` despite the standing repository authorization recorded in `AGENTS.md`, requiring a fresh explicit user confirmation in the active chat. No remote V5.4 branch, PR, CI run, APK or release is claimed; publication must resume only after that confirmation.
- 2026-08-18, Serge explicitly reconfirmed the connected GitHub authorization. Published the 19-file V5.4 tree through the GitHub app as remote commit `d2fca32bce42d3ceb6b7837ae3d499579ed5dc42` on `agent/v5-4-buyer-breath` and opened draft PR #49. GitHub Actions run #213 exposed one Kotlin parser ambiguity in the three phase-age predicates; fixed only those lambdas with explicit `LiveBreathingSample` parameters as remote commit `155f5b59b8537bcfc386c510a4f89f29c2a1a619`. No threshold, phase, action or risk rule changed.
- GitHub Actions run #214 (`32148181960`) then passed Kotlin/Android compilation, every unit test including the new buyer-breath lifecycle cases, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V5.4/code 83, launch-activity inspection, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V5.4-Intermediate-APK` ID `9328779122` is 6,375,147 bytes with archive SHA-256 `d29317589eaee9f14993c358994e7731cd78f9524836d7695afa073f990958f5`; its contained intermediate APK is 7,349,765 bytes with SHA-256 `e06f3ee10b0525745ecb4ed6010382677c36f00bb213c59c0ae37c4c87c1f69a`.
- Re-signed that exact CI APK with the installed-compatible personal update key. Final `PumpSignal-V5.4-Compatible-FINAL.apk` is 7,432,564 bytes with SHA-256 `6ee2888bc9c03afebffea1209157282448f3a160f0d831f087f51921406a6ff3`; APK Signature Schemes v2/v3 verify and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V5.3. All 843 non-signature ZIP payload entries are byte-identical to the CI APK. A clean repeat CI on this documentation head and merge to `main` remain required before final delivery.
- 2026-08-18 16:58 CEST, Codex Work Mode, V5.5 integrated-breath-curve checkpoint on `agent/v5-5-integrated-breath-curve`. Serge supplied screenshots and correctly reported that V5.4's dedicated `BuyerBreathActivity` substantially duplicated the established `CriticalOverviewActivity`. Removed the separate activity, manifest entry, main-screen button and all remaining references; the always-visible main summary now opens the single critical overview, whose existing eight factor bars, live PUMP/book/BTC lines and persistent now/5m/15m/30m/1h/6h breathing bars remain intact.
- Published the 13-file V5.5 tree as remote commit `4ebed3c024701f12b2b27c67ce75a31bcb244caa` on `agent/v5-5-integrated-breath-curve` and opened draft PR #50. GitHub Actions run #217 (`32152006388`) passed Kotlin/Android compilation, all unit tests including the adaptive timing-policy cases, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V5.5/code 84, launch-activity inspection, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V5.5-Intermediate-APK` ID `9330251271` is 6,378,172 bytes with archive SHA-256 `eda5f327a6b415f8ec14ae8bafdebcdeb62f4e9e68570b053f5dd3ec6f849256`; the contained intermediate APK is 7,353,013 bytes with SHA-256 `847a6edb07a387ce4cff1dd24e855d934408341d8dc0ed5d5d1e505378891192`.
- Re-signed that exact CI APK with the installed-compatible personal update key. Final `PumpSignal-V5.5-Compatible-FINAL.apk` is 7,436,660 bytes with SHA-256 `ab31ce7b719baa0c05b63898d16aaa8f7edeece808b93b893edd2a0cdd011be1`; APK Signature Schemes v2/v3 and ZIP integrity verify, and certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V5.4. All 843 non-signature payload entries are byte-identical to the CI APK. A clean repeat CI on this documentation head and merge to `main` remain required before final delivery.
- Added an adaptive time-labelled buyer-pressure arc inside `CriticalOverviewActivity`. Its solid segment is observed phase progress and its dashed segment is a historical timing guide; the current marker reports elapsed minutes, estimated total, buyer-flow peak, later price-response point and the next phase-recheck window. `BuyerBreathTimingPolicy` uses the V5.4 empirical anchors from 656 calm cycles (duration 20/35/65 minutes at p25/median/p75, median flow peak 10 minutes, median within-cycle price peak 15 minutes), blends them with the continuous current phase age, may stretch an unusually long live cycle to 90 minutes, leaves quiet at zero and disables ordinary timing in SHOCK. The curve describes buyer pressure rather than price and cannot execute BUY/EXIT or weaken existing multi-group/emergency protection.
- Advanced metadata once to V5.5/code 84, updated workflow artifact/package checks, README, durable rules and added `docs/V5_5_INTEGRATED_BREATH_CURVE_RU.md`. Extended `BuyerBreathCycleAnalyzerTest` with quiet, expansion, descending exhaustion, seller-completion and shock-timing cases. Static `git diff --check`, all Android XML parsing, workflow/version scans, Gradle-wrapper shell syntax and research-script Python syntax pass. Local Android compilation was unavailable because the isolated environment could not download Gradle; full unit tests, lint, assembly and package checks passed in GitHub Actions run #217. Compatible signing and payload verification are complete; repeat CI on the documentation head, merge and final APK delivery remain.
- 2026-08-18, Codex Work Mode, V5.6 full-chain audit on `agent/v5-6-audit-history`. Confirmed that V5.0 intentionally moved APP, DeepSig and DeepSigX into new preference stores while leaving V4.22 stores untouched; this preserved bytes but made V4 history invisible. Added an immutable V4 snapshot plus stable append-only SQLite performance ledger for APP, DeepSig, DeepSigX, FusionSim and Serge. Epoch balances/PnL remain separate while trades and decisions stay auditable across compatible updates and explicit account resets.
- Replaced the research-mode `КАНДИДАТ ВХОДА • WATCH • СДЕЛКА НЕ ЗАПРАШИВАЛАСЬ` card with an action-first Russian explanation for entry and exit states. Added policy tests for low readiness, rejected BUY, approved virtual BUY and ordinary position noise.
- Audited the foreground-service and WorkManager chains. The fresh base-market sync remains fail-closed, while evidence memory, ecosystem, Bitpanda read-only data, event radar, personal guard, DeepSig, position advisers, APP/DeepSig/Fusion paper accounts, notification dispatch and log writers are isolated so one optional failure cannot cancel later independent stages. Added failure records to both the operational journal and DeepSig activity.
- Added V4 archive and V4→V5+ ledger counts to the comparison screen and both datasets to the unified export. Added Bitpanda encrypted credentials to Android cloud/device-backup exclusions; research preferences, files and databases remain included. Advanced metadata once to V5.6/code 85 and updated CI package/artifact assertions.
- Initial V5.6 CI run #221 reached Kotlin compilation and found that the new personal-guard fallback returned the stored state instead of the guard outcome wrapper. Corrected both foreground and WorkManager paths to return `PersonalPositionGuardOutcome`; no signal threshold or trading rule changed.
- GitHub Actions run #222 (`32163130384`) passed Kotlin/Android compilation, all unit tests, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V5.6/code 85, launch-activity inspection, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V5.6-Intermediate-APK` ID `9334491874` is 6,406,325 bytes with archive SHA-256 `d673037d6381275a876436f27b17277a7d2f61624347c6cda7059ca7b8668c72`; the contained intermediate APK is 7,380,529 bytes with SHA-256 `5f0813bef1f9e9ab3950025228c934a7ba0d2d178458cd8644b3507b6109f5b1`.
- Re-signed that exact CI APK with the installed-compatible personal update key. Final `PumpSignal-V5.6-Compatible-FINAL.apk` is 7,465,332 bytes with SHA-256 `6c0076e1b465ba56ad4b9e5c13557b71b80c1449f2b11a1b3d539d2f46bdab8d`; APK Signature Schemes v2/v3 and ZIP integrity verify, certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, and all 843 non-signature payload entries are byte-identical to the CI APK. A clean documentation-head CI and merge to `main` remain before delivery.
- 2026-08-19, Codex Work Mode, V5.7 continuous-flow and missed-entry audit checkpoint on `agent/v5-7-continuous-flow-audit`. Replaced the categorical orange phase illustration inside the existing critical overview with a causal interactive six-hour path containing simultaneous 15/30/60/180/360-minute exponentially decayed layers plus a composite. A feed pause now retains the path and decays each layer toward zero instead of clearing the drawing. Added touch inspection, non-executing entry/exit review markers, capped BTC context at 5%, and retained the established bars and 24-hour breathing panel.
- Direct public Binance analysis of 18–19.08.2026 found an approximately +10.6% PUMPUSDT trough-to-peak move over 178 minutes (about 22:51–01:49 CEST) composed of multiple waves, while futures open-interest notional rose roughly $55.9m→$63.6m during the main advance. Source inspection explains likely structural blockers: closed-bar setup/anti-chase in APP, two-step proposal plus verification in DeepSig, three stable-anchor confirmations in DeepSigX, and a fresh read-only Bitpanda book plus executable DeepSig action in Fusion. The exact old phone state cannot be reconstructed retroactively, so V5.7 adds a durable 30-day per-participant entry-gate journal and unified-log export for future evidence-based audits.
- Added a capital-mechanism proxy combining price, spot/futures taker flow, OI and absorption to distinguish probable new longs, short covering, deleveraging, new shorts and absorbed buying. Public CEX data remains anonymous and the UI/DeepSig prompt explicitly forbid claiming a person, firm or country. Kept all V5 preference names, the V4 archive, performance ledger and breathing CSV unchanged; no account reset or new epoch occurs. Advanced metadata once to V5.7/code 86 and updated CI assertions, README, methodology, durable rules and focused tests. Local Gradle still cannot bootstrap because services.gradle.org is unavailable, so GitHub Actions compilation, full tests, lint and APK assembly remain mandatory.
- Published the exact V5.7 source tree `112c8b141c7ec670b355a8694af0dfb44603a261` as remote commit `b265f35ffc5e9ba198eb924dd00215eeba4b4c71` and opened draft PR #52. GitHub Actions run #225 (`32239218794`) passed Kotlin/Android compilation, all unit tests including continuous decay, BTC-cap, capital-mechanism and participant-gate cases, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V5.7/code 86, launch activity, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V5.7-Intermediate-APK` ID `9360244260` is 6,428,774 bytes with archive SHA-256 `6ac4fb15d56d904de59b313b38a1fa20500f1c628b7ad8982e722d9d92c7e2a2`; its contained APK is 7,405,213 bytes with SHA-256 `d30b6287ab8023a970f3b038af35eb0290b040370cf66b3b5e6c692dac6a6fbf`.
- Re-signed that exact CI APK with the installed-compatible personal update key. Final `PumpSignal-V5.7-Compatible-FINAL.apk` is 7,489,908 bytes with SHA-256 `0ab9a26101f92058f94492b796a8f92b90849f4a0398cfc526685caf77aa62e9`; APK Signature Schemes v2/v3 and ZIP integrity verify, certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V5.6, and all 843 non-signature payload entries are byte-identical to the CI APK. A clean repeat CI on this documentation head and merge to `main` remain required before delivery.
- 2026-08-20 10:45 CEST, Codex Work Mode, V5.8 flow-clock/large-footprint/Fusion checkpoint on `agent/v5-8-flow-clock-large-footprint`. Serge rejected the overlapping V5.7 wave chart and requested a clock-like human view plus a stronger indirect search for informed money. Replaced the five overlaid time series in the existing critical overview with one wide animated semicircular clock containing independent non-crossing 5m/15m/30m/60m tracks, direct seller/equilibrium/buyer zones and plain Russian phase labels; the established factor bars and 24-hour breathing history remain unchanged. Added a separate causal 5-minute layer and kept 15 minutes visually primary; 3h/6h remain structured DeepSig context rather than visual clutter.
- Added an anonymous large-flow fingerprint derived from the existing public Binance `aggTrade` tape: dynamic size threshold, largest buyer/seller taker orders, same-side similar-size slices within three minutes, total large notional and price-response absorption. The critical overview and DeepSig frame explicitly distinguish a public taker order/probable metaorder from trader identity; no person, firm, country, profit or repeated-account claim is made. The detector persists only its compact snapshot in the existing `micro_impulse_shadow_v1` store and does not rename/reset the 24-hour breathing CSV, V5 stores, V4 archive or SQLite performance ledger.
- Serge then specified an independent FusionSim paper strategy. BUY now requires fresh instant/5m/15m/30m scores all strictly positive. EXIT requires fresh instant/5m/15m plus a newly computed exact 20m layer all strictly negative, while 30m may remain positive. Fusion no longer waits for a DeepSig BUY/EXIT to trade its own virtual account, but still fails closed without a fresh read-only Bitpanda book and executes only virtually at ask/bid with fees. Added direct Fusion UI/audit explanations and focused entry/early-exit/staleness/large-slice/absorption/identity-limit tests. Advanced metadata once to V5.8/code 87 and updated workflow, README, AGENTS and methodology. `git diff --check` passes; local Gradle cannot bootstrap because `services.gradle.org` is unreachable, so clean GitHub Actions Kotlin compilation, full tests, lint and APK assembly are mandatory before signing or delivery.
- Serge explicitly authorized publishing V5.8 to the public `rosergenbg-debug/pump-paper-bot-android` repository, opening its PR and running GitHub Actions. Published the exact local 18-file tree `67520146f975a8aceba08631c6882f46e72cd4b5` as remote commit `aa3421689103c378d3a2f59166819ad80e9bfd55` on `agent/v5-8-flow-clock-large-footprint` and opened draft PR #53. GitHub Actions run #228 (`32350695336`) passed Kotlin/Android compilation, all unit tests including Fusion 20-minute early exit and anonymous large-order cases, lint, `assembleDebug`, package `com.example.pumppaperbot.v8`, V5.8/code 87, launch activity, intermediate v2 signature, ZIP integrity and artifact upload. Artifact `PumpSignal-V5.8-Intermediate-APK` ID `9399846279` is 6,442,081 bytes with archive SHA-256 `21c067b774c53c89d1667ab2a085ac9b7bfa34fa8f3ab14950d808ab159caa78`; its contained intermediate APK is 7,416,713 bytes with SHA-256 `14d7c868a6794d527712cba82df6bc012ffe779e8ba158d2e628dee6c94b5f9b`.
- Re-signed that exact CI APK with the installed-compatible personal update key. Final `PumpSignal-V5.8-Compatible-FINAL.apk` is 7,498,100 bytes with SHA-256 `6fe6841f93a1abde4c293c0180ec758cc45ac6c8be182b933dd62d5b52508756`; APK Signature Schemes v2/v3 and ZIP integrity verify, certificate SHA-256 is `1f778c4291c9d11c5f89f4de8773bda35a0125031adc05785daee23f27dc7823`, exactly matching V5.7, and all 843 non-signature payload entries are byte-identical to the CI APK. A clean repeat CI on this documentation head and merge to canonical `main` remain required before final delivery.
